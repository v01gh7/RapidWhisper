# Сборка RapidWhisper в .exe файл

Подробная инструкция по созданию исполняемого .exe файла для Windows.

## ⚠️ ВАЖНО: Безопасность API ключей

**Ваши API ключи НЕ попадут в .exe файл!**

Скрипт сборки (`build.bat`) автоматически:
1. Сохраняет ваш .env в backup
2. Создает чистый .env без ключей
3. Собирает .exe с чистым .env
4. Восстанавливает ваш .env

**Результат**: .exe содержит только пустые поля для ключей ✅

## Требования

- Windows 10/11
- Python 3.11+
- Установленный uv
- Все зависимости проекта

## Шаг 1: Установка PyInstaller

```bash
uv pip install pyinstaller
```

## Шаг 2: Создание spec файла

Создайте файл `RapidWhisper.spec` в корне проекта:

```python
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('.env.example', '.'),
    ],
    hiddenimports=[
        'PyQt6.QtCore',
        'PyQt6.QtGui',
        'PyQt6.QtWidgets',
        'pyaudio',
        'numpy',
        'keyboard',
        'pyperclip',
        'psutil',
        'openai',
        'dotenv',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='RapidWhisper',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # Без консоли
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=None,  # Добавьте путь к иконке если есть
)
```

## Шаг 3: Сборка

**ВАЖНО**: Скрипт автоматически создаст чистый .env без ваших ключей!

```bash
# Запустите build.bat (рекомендуется)
build.bat

# Или вручную
uv run pyinstaller RapidWhisper.spec
```

**Что происходит:**
1. Ваш текущий .env сохраняется в .env.backup
2. Создается чистый .env без API ключей
3. Собирается .exe с чистым .env
4. Ваш .env восстанавливается из backup

**Результат**: .exe НЕ содержит ваших API ключей! ✅


## Шаг 4: Результат

После успешной сборки:
- Исполняемый файл: `dist/RapidWhisper.exe`
- Размер: ~150-200 MB (включает Python и все библиотеки)

## Шаг 5: Подготовка к распространению

### Создайте папку для распространения:

```
RapidWhisper/
├── RapidWhisper.exe
├── .env.example
└── README.txt
```

### Создайте README.txt:

```
RapidWhisper v1.1.0
===================

Быстрая транскрипция речи с микрофона

УСТАНОВКА:
1. Скопируйте .env.example в .env
2. Откройте .env в блокноте
3. Добавьте ваш API ключ:
   - Groq (рекомендуется): https://console.groq.com/keys
   - OpenAI: https://platform.openai.com/api-keys
   - GLM: https://open.bigmodel.cn/usercenter/apikeys
4. Запустите RapidWhisper.exe

ИСПОЛЬЗОВАНИЕ:
- Ctrl+Space - начать/остановить запись
- ESC - отменить запись
- Правый клик на трее - настройки

ТРЕБОВАНИЯ:
- Windows 10/11
- Микрофон
- Интернет-соединение

© 2026 RapidWhisper
```

## Альтернатива: Однофайловая сборка

Для создания одного .exe файла без папок:

```bash
uv run pyinstaller --onefile --windowed --name RapidWhisper main.py
```

**Плюсы:**
- Один файл
- Легко распространять

**Минусы:**
- Медленнее запускается (распаковка при каждом запуске)
- Больше размер

## Оптимизация размера

### Вариант 1: UPX компрессия

```bash
# Установите UPX
# Скачайте с https://upx.github.io/

# В spec файле уже включено:
upx=True
```

### Вариант 2: Исключение ненужных модулей

Добавьте в `excludes` в spec файле:

```python
excludes=[
    'matplotlib',
    'scipy',
    'pandas',
    'PIL',
    'tkinter',
],
```

## Решение проблем

### Ошибка: "Failed to execute script"

**Решение:**
1. Соберите с консолью для отладки:
   ```python
   console=True  # В spec файле
   ```
2. Запустите и проверьте ошибки

### Ошибка: "No module named..."

**Решение:**
Добавьте модуль в `hiddenimports`:
```python
hiddenimports=[
    'missing_module_name',
],
```

### Антивирус блокирует .exe

**Решение:**
1. Добавьте в исключения антивируса
2. Подпишите .exe цифровой подписью (для распространения)

### Большой размер файла

**Решение:**
1. Используйте виртуальное окружение только с нужными пакетами
2. Включите UPX компрессию
3. Исключите ненужные модули

## Автоматизация сборки

Создайте `build.bat`:

```batch
@echo off
echo Сборка RapidWhisper...
uv run pyinstaller RapidWhisper.spec --clean
echo.
echo Готово! Файл: dist\RapidWhisper.exe
pause
```

Запустите двойным кликом.

## Тестирование .exe

1. Скопируйте `dist/RapidWhisper.exe` в новую папку
2. Скопируйте `.env` с вашими ключами
3. Запустите RapidWhisper.exe
4. Проверьте все функции:
   - Запись аудио
   - Транскрипция
   - Настройки
   - Горячие клавиши

## Распространение

### Вариант 1: ZIP архив

```bash
# Создайте архив
7z a RapidWhisper-v1.1.0.zip dist/RapidWhisper.exe .env.example README.txt
```

### Вариант 2: Installer (NSIS)

Создайте installer с помощью NSIS:
- Автоматическая установка
- Ярлык на рабочем столе
- Автозапуск
- Удаление через "Программы и компоненты"

### Вариант 3: Portable

Просто папка с .exe - работает без установки.

## Обновления

При выпуске новой версии:

1. Обновите версию в коде
2. Пересоберите .exe
3. Создайте changelog
4. Упакуйте и распространите

## Checklist перед релизом

- [ ] Все тесты проходят
- [ ] .exe собирается без ошибок
- [ ] .exe запускается на чистой системе
- [ ] Все функции работают
- [ ] Настройки сохраняются
- [ ] Горячие клавиши работают
- [ ] Нет критических ошибок в логах
- [ ] README.txt актуален
- [ ] .env.example содержит все параметры
- [ ] Версия обновлена

## Полезные команды

```bash
# Очистка перед сборкой
uv run pyinstaller RapidWhisper.spec --clean

# Сборка с логами
uv run pyinstaller RapidWhisper.spec --log-level DEBUG

# Проверка зависимостей
uv run pyinstaller --collect-all PyQt6 main.py

# Анализ размера
uv run pyinstaller --analyze RapidWhisper.spec
```

## Дополнительные ресурсы

- PyInstaller документация: https://pyinstaller.org/
- UPX: https://upx.github.io/
- NSIS: https://nsis.sourceforge.io/

---

**Готово!** Теперь у вас есть `RapidWhisper.exe` готовый к использованию! 🎉
