# Горячая перезагрузка настроек

## Обзор

RapidWhisper поддерживает **горячую перезагрузку настроек** - вы можете изменять настройки без перезапуска приложения! Большинство параметров применяются мгновенно.

## Как это работает

1. Откройте окно настроек (правый клик на трее → Настройки)
2. Измените нужные параметры
3. Нажмите "💾 Сохранить"
4. Настройки применяются **немедленно** без перезапуска!

## Какие настройки применяются мгновенно

### ✅ Применяются без перезапуска

#### AI Provider
- **Провайдер AI** (groq, openai, glm)
- **API ключи** для всех провайдеров
- Новый провайдер будет использован при следующей транскрипции

#### Определение тишины
- **Порог тишины** (SILENCE_THRESHOLD)
- **Длительность тишины** (SILENCE_DURATION)
- Новые значения применяются при следующей записи

#### Горячие клавиши
- **Горячая клавиша** (HOTKEY)
- Старая клавиша отменяется, новая регистрируется мгновенно
- Можно сразу использовать новую комбинацию

### ⚠️ Требуют перезапуска

Следующие параметры требуют полного перезапуска приложения:

#### Параметры аудио
- **Частота дискретизации** (SAMPLE_RATE)
- **Размер чанка** (CHUNK_SIZE)
- Эти параметры влияют на инициализацию аудио движка

#### Параметры интерфейса
- **Автоскрытие** (AUTO_HIDE_DELAY)
- Используется при инициализации окна

**Примечание**: Окно настроек покажет уведомление если какие-то параметры требуют перезапуска.

## Процесс горячей перезагрузки

### Что происходит при сохранении

1. **Валидация** - проверка корректности новых значений
2. **Сохранение** - запись в файл `.env`
3. **Перезагрузка** - загрузка новой конфигурации
4. **Применение** - обновление компонентов:
   - Детектор тишины пересоздается с новыми параметрами
   - Горячие клавиши перерегистрируются
   - AI провайдер обновляется для следующего запроса
5. **Уведомление** - показ сообщения об успехе

### Обработка ошибок

Если в новых настройках есть ошибки:
- Показывается уведомление с описанием проблемы
- Старые настройки остаются активными
- Проверьте логи в `rapidwhisper.log` для деталей

## Примеры использования

### Пример 1: Смена AI провайдера

**Ситуация**: Хотите попробовать OpenAI вместо Groq

1. Откройте настройки
2. Вкладка "🤖 AI Provider"
3. Выберите "openai" в выпадающем списке
4. Вставьте OpenAI API ключ
5. Нажмите "💾 Сохранить"
6. ✅ Следующая транскрипция будет использовать OpenAI!

### Пример 2: Настройка тишины

**Ситуация**: Запись останавливается слишком быстро

1. Откройте настройки
2. Вкладка "⚡ Приложение"
3. Увеличьте "Длительность тишины" до 2.5 секунд
4. Нажмите "💾 Сохранить"
5. ✅ Следующая запись будет ждать дольше!

### Пример 3: Смена горячей клавиши

**Ситуация**: Ctrl+Space конфликтует с другим приложением

1. Откройте настройки
2. Вкладка "⚡ Приложение"
3. Измените "Горячая клавиша" на "F2"
4. Нажмите "💾 Сохранить"
5. ✅ Сразу можете использовать F2!

### Пример 4: Обновление API ключа

**Ситуация**: API ключ истек, нужен новый

1. Откройте настройки
2. Вкладка "🤖 AI Provider"
3. Вставьте новый API ключ
4. Нажмите "💾 Сохранить"
5. ✅ Новый ключ используется немедленно!

## Технические детали

### Компоненты которые обновляются

```python
# 1. Детектор тишины
self.silence_detector = SilenceDetector(
    threshold=new_config.silence_threshold,
    silence_duration=new_config.silence_duration
)

# 2. Горячие клавиши
self.hotkey_manager.unregister_hotkey()  # Отменить старую
self.hotkey_manager.register_hotkey(new_config.hotkey)  # Зарегистрировать новую

# 3. AI Provider
# Обновляется автоматически при следующем запросе
```

### Логирование

Все изменения логируются в `rapidwhisper.log`:

```
23:45:12 - INFO - Перезагрузка настроек...
23:45:12 - INFO - Детектор тишины обновлен: threshold=0.03, duration=2.5
23:45:12 - INFO - Обновление горячей клавиши: ctrl+space -> F2
23:45:12 - INFO - Горячая клавиша обновлена: F2
23:45:12 - INFO - AI Provider изменен: groq -> openai
23:45:12 - INFO - API ключ для openai загружен: sk-proj-ab...
23:45:12 - INFO - Настройки успешно перезагружены
```

## Преимущества горячей перезагрузки

### ✅ Удобство
- Не нужно закрывать и открывать приложение
- Мгновенное применение изменений
- Можно экспериментировать с настройками

### ✅ Безопасность
- Валидация перед применением
- Откат к старым настройкам при ошибке
- Подробное логирование изменений

### ✅ Производительность
- Не теряется состояние приложения
- Не нужно ждать полной инициализации
- Горячие клавиши остаются зарегистрированными

## Ограничения

### Что НЕ обновляется горячо

1. **Параметры аудио движка** (sample_rate, chunk_size)
   - Требуют пересоздания аудио потока
   - Перезапустите приложение для применения

2. **Параметры окна** (window_width, window_height)
   - Требуют пересоздания UI
   - Перезапустите приложение для применения

3. **Параметры логирования** (log_level, log_file)
   - Требуют переинициализации логгера
   - Перезапустите приложение для применения

## Советы

### Экспериментируйте безопасно

1. Измените один параметр
2. Сохраните и протестируйте
3. Если не понравилось - верните обратно
4. Сохраните снова

### Проверяйте логи

Если что-то работает не так:
1. Откройте `rapidwhisper.log`
2. Найдите строки с "Перезагрузка настроек"
3. Проверьте сообщения об ошибках

### Используйте уведомления

Приложение показывает уведомления:
- ✅ "Настройки обновлены" - успех
- ⚠️ "Ошибка настроек" - проблема с валидацией
- ❌ "Ошибка" - критическая ошибка

## FAQ

**Q: Нужно ли перезапускать после каждого изменения?**
A: Нет! Большинство настроек применяются мгновенно.

**Q: Что делать если настройки не применились?**
A: Проверьте логи в `rapidwhisper.log` и уведомления в трее.

**Q: Можно ли откатить изменения?**
A: Да, просто откройте настройки и верните старые значения.

**Q: Безопасно ли менять настройки во время записи?**
A: Да, но новые настройки применятся только при следующей записи.

**Q: Что происходит с текущей записью при смене настроек?**
A: Текущая запись продолжается со старыми настройками, новые применятся при следующей.
