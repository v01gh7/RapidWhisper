# RapidWhisper - Быстрая транскрипция речи

Современное приложение для преобразования речи в текст с использованием AI API (Groq, OpenAI, GLM).

## ✨ Возможности

- 🎤 **Глобальная горячая клавиша** - Активация из любого приложения (Ctrl+Space)
- 🎨 **Современный интерфейс** - Минималистичное плавающее окно с эффектом матового стекла
- 🌊 **Визуализация в реальном времени** - Анимированная звуковая волна во время записи
- 🤫 **Умное определение тишины** - Автоматическая остановка после пауз
- ⚡ **Быстрая транскрипция** - 1-2 секунды через AI API
- 📋 **Автокопирование** - Результат мгновенно копируется в буфер обмена
- 🧵 **Многопоточность** - Плавный интерфейс с фоновой обработкой
- ⚙️ **Окно настроек** - Удобная настройка через графический интерфейс
- 🔔 **Системный трей** - Работа в фоне с уведомлениями
- 🚫 **Отмена записи** - Нажмите ESC для отмены без транскрипции

## 📋 Требования

- Python 3.11 или выше
- Доступ к микрофону
- Интернет-соединение для AI API
- API ключ от одного из провайдеров:
  - **Groq** (рекомендуется, бесплатный): https://console.groq.com/keys
  - **OpenAI**: https://platform.openai.com/api-keys
  - **GLM**: https://open.bigmodel.cn/usercenter/apikeys

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd RapidWhisper
```

### 2. Создайте виртуальное окружение с uv

```bash
# Установите uv если еще не установлен
pip install uv

# Создайте и активируйте виртуальное окружение
uv venv
# На Windows:
.venv\Scripts\activate
```

### 3. Установите зависимости

```bash
uv pip install -e .
```

### 4. Настройте API ключ

Скопируйте пример файла окружения:

```bash
copy .env.example .env
```

Отредактируйте `.env` и настройте провайдер:

```env
# Выберите AI провайдер (groq, openai, или glm)
AI_PROVIDER=groq

# Добавьте API ключ для выбранного провайдера
GROQ_API_KEY=ваш_ключ_groq
# OPENAI_API_KEY=ваш_ключ_openai
# GLM_API_KEY=ваш_ключ_glm
```

**Или используйте окно настроек** после первого запуска для настройки через GUI.

## 💡 Использование

### Запуск приложения

```bash
python main.py
```

### Базовый рабочий процесс

1. Нажмите **Ctrl+Space** для активации плавающего окна
2. Говорите в микрофон
3. Нажмите **Ctrl+Space** снова для остановки записи, или дождитесь автоматического определения тишины
4. Нажмите **ESC** для отмены записи без транскрипции
5. Транскрибированный текст автоматически копируется в буфер обмена
6. Вставьте в любое место с помощью **Ctrl+V**
7. Уведомление появится в системном трее когда транскрипция завершена

### ⚙️ Настройки

Все настройки можно изменить через **Окно настроек** или редактируя файл `.env`:

#### Открытие окна настроек

1. Правый клик на иконке в трее
2. Выберите "Настройки"
3. Измените настройки в графическом интерфейсе
4. Нажмите "💾 Сохранить"
5. Перезапустите приложение для применения изменений

#### Категории настроек

**🤖 AI Provider**
- Выбор между Groq (бесплатный и быстрый), OpenAI или GLM
- Настройка API ключей для каждого провайдера
- Groq рекомендуется для начинающих (есть бесплатный тариф)

**⚡ Приложение**
- `HOTKEY` - Глобальная горячая клавиша (по умолчанию: ctrl+space)
- `SILENCE_THRESHOLD` - Чувствительность определения тишины (по умолчанию: 0.02)
- `SILENCE_DURATION` - Длительность тишины перед остановкой (по умолчанию: 1.5 секунды)
- `AUTO_HIDE_DELAY` - Задержка автоскрытия окна (по умолчанию: 2.5 секунды)

**🎤 Аудио**
- `SAMPLE_RATE` - Частота дискретизации (по умолчанию: 16000 Гц, рекомендуется для речи)
- `CHUNK_SIZE` - Размер аудио чанка (по умолчанию: 1024 фрейма)

Подробное руководство: `docs/settings_guide.md`

## 🎯 Рекомендуемые настройки

### Для быстрой работы
- **AI Provider**: Groq
- **Silence Duration**: 1.5 секунды
- **Sample Rate**: 16000 Гц

### Для высокого качества
- **AI Provider**: OpenAI
- **Silence Duration**: 2.0 секунды
- **Sample Rate**: 16000 Гц

### Для шумной среды
- **Silence Threshold**: 0.03-0.05
- **Silence Duration**: 2.5-3.0 секунды

### Для тихой среды
- **Silence Threshold**: 0.01-0.02
- **Silence Duration**: 1.0-1.5 секунды

## 🔧 Решение проблем

### Проблемы с микрофоном

**Ошибка: "Микрофон занят другим приложением"**
- Закройте другие приложения использующие микрофон
- Проверьте системные настройки аудио
- Перезапустите приложение

### Проблемы с API

**Ошибка: "API ключ не найден"**
- Проверьте правильность API ключа в `.env` или окне настроек
- Убедитесь что файл `.env` существует в корне проекта
- Проверьте что нет лишних пробелов вокруг ключа

**Ошибка: "Ошибка сети"**
- Проверьте интернет-соединение
- Убедитесь что AI API сервис доступен
- Проверьте настройки файрвола

### Проблемы с горячими клавишами

**Горячая клавиша не работает**
- Попробуйте другую клавишу в настройках (например, F2, F3)
- Проверьте не использует ли другое приложение ту же клавишу
- Запустите приложение от имени администратора (Windows)

## 📁 Структура проекта

```
RapidWhisper/
├── core/           # Конфигурация и управление состоянием
├── ui/             # Компоненты интерфейса PyQt6
├── services/       # Аудио, API и сервисные компоненты
├── models/         # Модели данных
├── utils/          # Утилиты и логирование
├── tests/          # Юнит, property и интеграционные тесты
├── docs/           # Документация
├── main.py         # Точка входа приложения
└── pyproject.toml  # Конфигурация проекта
```

## 🧪 Разработка

### Запуск тестов

```bash
# Запустить все тесты
pytest

# Запустить с покрытием
pytest --cov=. --cov-report=html

# Запустить только юнит-тесты
pytest tests/unit/

# Запустить только property-based тесты
pytest tests/property/
```

### Качество кода

```bash
# Форматирование кода
black .

# Линтинг
ruff check .

# Проверка типов
mypy .
```

## 📝 Лицензия

[Добавьте вашу лицензию здесь]

## 🤝 Вклад

Вклад приветствуется! Не стесняйтесь отправлять Pull Request.

## 🙏 Благодарности

- Вдохновлено [SuperWhisper](https://superwhisper.com/)
- Работает на [Groq](https://groq.com/), [OpenAI](https://openai.com/), [GLM](https://open.bigmodel.cn/)
- Создано с [PyQt6](https://www.riverbankcomputing.com/software/pyqt/)
