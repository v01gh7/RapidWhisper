# Комбинированный режим обработки текста

## Обзор

RapidWhisper поддерживает интеллектуальную комбинацию постобработки и форматирования текста в **один API запрос**, что экономит время и деньги. Система также применяет **универсальное базовое форматирование** для неизвестных приложений.

## Режимы работы

### 1. Только постобработка
**Условия**: 
- `POST_PROCESSING_ENABLED=true`
- `FORMATTING_ENABLED=false`

**Поведение**: Отправляется один запрос с промптом постобработки для исправления грамматики и пунктуации.

### 2. Только форматирование (с совпадением приложения)
**Условия**:
- `FORMATTING_ENABLED=true`
- `POST_PROCESSING_ENABLED=false`
- Активное приложение соответствует настроенному формату (например, Notion, Obsidian, Markdown)

**Поведение**: Отправляется один запрос с промптом форматирования для структурирования текста под конкретное приложение.

### 3. Только форматирование (без совпадения приложения) - НОВОЕ! ✨
**Условия**:
- `FORMATTING_ENABLED=true`
- `POST_PROCESSING_ENABLED=false`
- Активное приложение НЕ соответствует ни одному настроенному формату

**Поведение**: Отправляется один запрос с **универсальным базовым промптом форматирования**, который:
- Разделяет текст на предложения
- Добавляет переносы строк между предложениями
- Группирует связанные предложения в параграфы
- Выделяет заголовки, если упоминаются темы
- Форматирует списки, если перечисляются элементы
- Делает текст читаемым без привязки к конкретному приложению

**Преимущества**:
- 📝 Текст всегда форматируется, даже в неизвестных приложениях
- 🎯 Базовое форматирование делает текст читаемым
- ⚡ Один запрос - быстро и эффективно

### 4. Комбинированный режим (с совпадением приложения)
**Условия**:
- `POST_PROCESSING_ENABLED=true`
- `FORMATTING_ENABLED=true`
- Активное приложение соответствует настроенному формату

**Поведение**: Отправляется **ОДИН** комбинированный запрос, который:
1. Сначала исправляет грамматику и пунктуацию (постобработка)
2. Затем структурирует текст под формат приложения (форматирование)

**Преимущества**:
- ⚡ Быстрее - один запрос вместо двух
- 💰 Дешевле - экономия на API вызовах
- 🎯 Лучше - AI обрабатывает текст целостно

### 5. Комбинированный режим (без совпадения приложения) - НОВОЕ! ✨
**Условия**:
- `POST_PROCESSING_ENABLED=true`
- `FORMATTING_ENABLED=true`
- Активное приложение НЕ соответствует ни одному настроенному формату

**Поведение**: Отправляется **ОДИН** комбинированный запрос с постобработкой + универсальным базовым форматированием.

**Преимущества**:
- 📝 Текст всегда обрабатывается, даже в неизвестных приложениях
- ⚡ Один запрос - максимальная эффективность
- 🎯 Комбинация постобработки и базового форматирования

## Как это работает

### Логика определения режима

```python
# В ProcessingCoordinator
if formatting_enabled and format_match:
    if post_processing_enabled:
        # КОМБИНИРОВАННЫЙ РЕЖИМ
        combined_prompt = f"{post_processing_prompt}\n\n{formatting_prompt}"
        result = ai_client.post_process_text(text, combined_prompt)
    else:
        # ТОЛЬКО ФОРМАТИРОВАНИЕ
        result = formatting_module.format_text(text, format_type)
elif post_processing_enabled:
    # ТОЛЬКО ПОСТОБРАБОТКА
    result = ai_client.post_process_text(text, post_processing_prompt)
else:
    # НЕТ ОБРАБОТКИ
    result = text
```

### Пример комбинированного промпта

```
You are a text editor. Your task: fix grammatical errors, add punctuation and improve text readability.

CRITICAL INSTRUCTIONS:
1. PRESERVE ALL CONTENT: Keep every word from the original text
2. ADD STRUCTURE: Actively identify and create proper formatting
3. NO NEW CONTENT: Do not add examples, explanations, or text that wasn't spoken

Task: Transform the transcribed speech into well-structured text.

Your job:
- ANALYZE the content and identify natural sections
- CREATE headings where appropriate for main topics and subtopics
- CONVERT lists when the speaker mentions multiple items
- ADD emphasis for important points
- INSERT line breaks between logical sections
- STRUCTURE the content for maximum readability

Remember: Use ALL the original words, just organize them better.

Output ONLY the reformatted text.
```

## Логи для диагностики

При комбинированном режиме в логах появляются следующие сообщения:

```
INFO - Both formatting and post-processing enabled with format match: markdown
INFO - COMBINED MODE: Formatting + Post-processing in single API call
INFO - Combined prompt created (length: 983)
INFO - ✅ Combined processing completed successfully
```

## Настройка

### Включить комбинированный режим

1. В `.env` файле:
```env
POST_PROCESSING_ENABLED=true
FORMATTING_ENABLED=true
FORMATTING_APPLICATIONS=notion,obsidian,markdown,word,libreoffice,vscode
```

2. Откройте одно из настроенных приложений (например, Obsidian или файл .md)

3. Запишите текст - он будет обработан в комбинированном режиме

### Отключить комбинированный режим

Установите один из параметров в `false`:
```env
POST_PROCESSING_ENABLED=false  # Только форматирование
# ИЛИ
FORMATTING_ENABLED=false  # Только постобработка
```

## Производительность

| Режим | Количество запросов | Время обработки | Стоимость |
|-------|-------------------|-----------------|-----------|
| Постобработка + Форматирование (раздельно) | 2 | ~2-4 сек | 2x |
| Комбинированный режим | 1 | ~1-2 сек | 1x |
| Экономия | -50% | -50% | -50% |

## Устранение неполадок

### Комбинированный режим не активируется

**Проверьте**:
1. Оба параметра включены в `.env`
2. Активное приложение соответствует списку в `FORMATTING_APPLICATIONS`
3. В логах есть сообщение "COMBINED MODE"

### Текст обрабатывается дважды

Если видите два запроса в логах, проверьте версию `ProcessingCoordinator` - старые версии могли отправлять два запроса.

## Дополнительная информация

- [Руководство по форматированию](FORMATTING_UI_GUIDE.md)
- [Настройка постобработки](settings_guide.md)
- [Пользовательские промпты](FORMATTING_FEATURE.md)
