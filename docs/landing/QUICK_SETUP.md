# 🚀 Быстрая настройка лендинга

## Шаг 1: Замените username

Замените `V01GH7` на ваш GitHub username во всех файлах лендинга.

**Быстрая команда (PowerShell):**
```powershell
cd docs/landing
$user = "ваш-username"  # Замените на свой
(Get-Content index.html) -replace 'V01GH7', $user | Set-Content index.html
(Get-Content docs.html) -replace 'V01GH7', $user | Set-Content docs.html
```

## Шаг 2: Создайте релиз

### Через GitHub Actions:
1. GitHub → Actions → "Build RapidWhisper"
2. "Run workflow" → Выберите ветку → "Run workflow"
3. Дождитесь завершения (15-20 минут)
4. GitHub → Releases → Найдите draft release
5. Отредактируйте и опубликуйте (снимите галочку "Draft")

### Важно для релиза:
- Имена файлов должны быть точно:
  - `RapidWhisper.exe` (Windows)
  - `RapidWhisper-macOS.dmg` (macOS)
  - `RapidWhisper` (Linux)

## Шаг 3: Проверьте ссылки

Откройте `index.html` и проверьте, что кнопки скачивания работают.

## Готово! 🎉

Ваш лендинг теперь автоматически скачивает последние версии из GitHub Releases.

### Ссылки работают так:
```
https://github.com/USERNAME/rapidwhisper/releases/latest/download/FILENAME
```

При каждом новом релизе ссылки автоматически обновляются на последнюю версию!
