# Changelog: Кнопка сброса горячей клавиши

## Дата: 2026-01-30

## Изменения

### 1. Добавлена кнопка сброса
- **Файл**: `ui/settings_window.py`
- Кнопка с иконкой 🔄 рядом с полем горячей клавиши
- Максимальная ширина: 40px
- Курсор "рука" при наведении
- Tooltip: "Сбросить на текущее сохраненное значение"

### 2. Функциональность кнопки
- При нажатии загружает текущее сохраненное значение из конфигурации
- Устанавливает его в поле ввода
- Убирает фокус с поля (предотвращает случайную перезапись)
- Логирует действие в консоль

### 3. Метод `_reset_hotkey()`
```python
def _reset_hotkey(self):
    """Сбрасывает горячую клавишу на текущее сохраненное значение."""
    current_hotkey = self.config.hotkey
    self.hotkey_edit.setText(current_hotkey)
    self.hotkey_edit.clearFocus()
    logger.info(f"Горячая клавиша сброшена на: {current_hotkey}")
```

## Проблема (от пользователя)

"Я сейчас выбрал другое сочетание клавиши для теста и хотел вернуть Ctrl+Space. И поскольку это активная кнопка для записи, оно начало мне записывать."

## Решение

Теперь можно:
1. Выбрать новое сочетание для теста (например, Ctrl+J)
2. Нажать кнопку 🔄 справа от поля
3. Значение автоматически вернется к сохраненному (например, ctrl+space)
4. Не нужно вручную нажимать Ctrl+Space (что запускало бы запись)

## Использование

### Сценарий 1: Тестирование новой клавиши
1. Открыть настройки → Приложение
2. Кликнуть на поле горячей клавиши
3. Нажать новое сочетание (например, Ctrl+J)
4. Протестировать (закрыть настройки, попробовать Ctrl+J)
5. Если не понравилось - открыть настройки снова
6. Нажать кнопку 🔄
7. Значение вернется к ctrl+space
8. Сохранить настройки

### Сценарий 2: Случайное нажатие
1. Открыть настройки → Приложение
2. Случайно кликнуть на поле горячей клавиши
3. Случайно нажать какую-то клавишу
4. Нажать кнопку 🔄
5. Значение вернется к сохраненному
6. Закрыть настройки без сохранения

### Сценарий 3: Отмена изменений
1. Открыть настройки → Приложение
2. Изменить горячую клавишу
3. Передумать
4. Нажать кнопку 🔄
5. Значение вернется к сохраненному
6. Можно продолжить редактирование других настроек

## Визуальное расположение

```
┌─────────────────────────────────────────┐
│ Горячая клавиша:                        │
│ ┌──────────────────────────┐  ┌────┐   │
│ │ ctrl+space               │  │ 🔄 │   │
│ └──────────────────────────┘  └────┘   │
└─────────────────────────────────────────┘
```

## Технические детали

### Layout структура
```python
hotkey_container = QHBoxLayout()
├── self.hotkey_edit (HotkeyInput)
└── reset_hotkey_btn (QPushButton)
```

### Стиль кнопки
- Наследует стиль от общего QPushButton
- Максимальная ширина: 40px
- Иконка: 🔄 (Unicode символ)
- Курсор: PointingHandCursor

### Логика сброса
1. Читает `self.config.hotkey` (текущее сохраненное значение)
2. Устанавливает в `self.hotkey_edit.setText()`
3. Убирает фокус `self.hotkey_edit.clearFocus()`
4. Логирует действие

## Преимущества

✅ **Удобство**: Быстрый возврат к сохраненному значению  
✅ **Безопасность**: Не нужно нажимать активную горячую клавишу  
✅ **Интуитивность**: Иконка 🔄 понятна без объяснений  
✅ **Гибкость**: Можно тестировать разные клавиши без страха  

## Файлы изменены

- ✅ `ui/settings_window.py` - добавлена кнопка и метод
- ✅ `CHANGELOG_HOTKEY_RESET.md` - эта документация

## Статус

🟢 **Готово к использованию**

Кнопка сброса добавлена и работает корректно.
