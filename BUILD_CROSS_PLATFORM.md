# 🚀 Кросс-платформенная сборка RapidWhisper

## 📋 Содержание
- [Автоматическая сборка (GitHub Actions)](#автоматическая-сборка-github-actions)
- [Ручная сборка на macOS](#ручная-сборка-на-macos)
- [Юридические аспекты](#юридические-аспекты)
- [FAQ](#faq)

---

## 🤖 Автоматическая сборка (GitHub Actions)

### Преимущества:
✅ **Бесплатно** для публичных репозиториев  
✅ **Автоматически** собирает для Windows, macOS и Linux  
✅ **Не нужен Mac** - GitHub предоставляет виртуальные машины  
✅ **Воспроизводимые сборки** - всегда одинаковый результат  

### Как использовать:

#### 1. Загрузить код на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/RapidWhisper.git
git push -u origin main
```

#### 2. Запустить сборку

**Автоматически:**
- При каждом `git push` в ветку `main`
- При создании Pull Request

**Вручную:**
1. Откройте GitHub → ваш репозиторий
2. Перейдите в **Actions**
3. Выберите **Build RapidWhisper**
4. Нажмите **Run workflow**

#### 3. Скачать собранные файлы

После завершения сборки (5-10 минут):
1. Откройте **Actions** → последний запуск
2. Прокрутите вниз до **Artifacts**
3. Скачайте:
   - `RapidWhisper-Windows` - .exe для Windows
   - `RapidWhisper-macOS` - .dmg для macOS
   - `RapidWhisper-Linux` - бинарник для Linux

#### 4. Создать релиз (опционально)

Для создания официального релиза:
```bash
git tag v1.0.0
git push origin v1.0.0
```

GitHub автоматически:
- Соберёт все версии
- Создаст Release на странице проекта
- Прикрепит файлы к релизу

---

## 🍎 Ручная сборка на macOS

Если у вас есть доступ к Mac (свой или друга):

### 1. Установить зависимости
```bash
# Установить Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Python
brew install python@3.13

# Установить uv
pip3 install uv

# Клонировать проект
git clone https://github.com/ваш-username/RapidWhisper.git
cd RapidWhisper

# Установить зависимости
uv pip install -r requirements.txt
uv pip install pyinstaller
```

### 2. Собрать приложение
```bash
pyinstaller RapidWhisper.spec --clean
```

### 3. Создать .app bundle (опционально)
```bash
# Создать структуру .app
mkdir -p "dist/RapidWhisper.app/Contents/MacOS"
mv dist/RapidWhisper "dist/RapidWhisper.app/Contents/MacOS/"

# Создать Info.plist
cat > "dist/RapidWhisper.app/Contents/Info.plist" << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>RapidWhisper</string>
    <key>CFBundleIdentifier</key>
    <string>com.rapidwhisper.app</string>
    <key>CFBundleName</key>
    <string>RapidWhisper</string>
    <key>CFBundleVersion</key>
    <string>1.0.0</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>
    <key>LSMinimumSystemVersion</key>
    <string>10.15</string>
    <key>NSHighResolutionCapable</key>
    <true/>
</dict>
</plist>
EOF

# Скопировать иконку (если есть)
mkdir -p "dist/RapidWhisper.app/Contents/Resources"
cp public/RapidWhisper.icns "dist/RapidWhisper.app/Contents/Resources/" 2>/dev/null || true
```

### 4. Создать DMG (опционально)
```bash
hdiutil create -volname "RapidWhisper" \
    -srcfolder "dist/RapidWhisper.app" \
    -ov -format UDZO \
    "dist/RapidWhisper-macOS.dmg"
```

### 5. Подписать приложение (для распространения)

Для распространения вне App Store нужна подпись разработчика:

```bash
# Получить Apple Developer ID (платно, $99/год)
# https://developer.apple.com/programs/

# Подписать приложение
codesign --deep --force --verify --verbose \
    --sign "Developer ID Application: Ваше Имя (TEAM_ID)" \
    "dist/RapidWhisper.app"

# Нотаризовать (отправить Apple на проверку)
xcrun notarytool submit "dist/RapidWhisper-macOS.dmg" \
    --apple-id "ваш@email.com" \
    --password "app-specific-password" \
    --team-id "TEAM_ID" \
    --wait
```

**Примечание**: Без подписи пользователи увидят предупреждение "неизвестный разработчик". Обход: правый клик → Открыть.

---

## ⚖️ Юридические аспекты

### Авторские права

**Вы остаётесь автором**, даже если кто-то собирает проект:
- ✅ Код написан вами → вы автор
- ✅ Компиляция не передаёт права
- ✅ LICENSE файл защищает ваши права

### Защита прав

**1. Добавьте LICENSE файл** (уже добавлен)
- **Proprietary Software License - Personal Use Only**
- Разрешает только личное использование готовой программы
- Запрещает доступ к исходному коду
- Запрещает коммерческое использование и распространение
- Полный контроль автора над проектом

**Альтернативные лицензии** (если захотите изменить):
- **Freeware** - бесплатно, закрытый код
- **MIT License** - открытый код, разрешает всё
- **GPL** - открытый код, требует открытости форков
- **Apache 2.0** - открытый код + защита от патентов
- **CC BY-NC-ND** - Creative Commons некоммерческая

**2. Добавьте Copyright в код**

В начало `main.py`:
```python
"""
RapidWhisper - Voice Transcription Application

Copyright (c) 2026 [Ваше имя]
Licensed under the MIT License
"""
```

**3. Письменное соглашение** (если просите кого-то собрать)

Простой шаблон:
```
Я, [Имя помощника], собираю проект RapidWhisper по просьбе [Ваше имя].

Подтверждаю:
- Все права на код принадлежат [Ваше имя]
- Я не претендую на авторские права
- Я выполняю только техническую работу по компиляции

Дата: [дата]
Подпись: [подпись или email]
```

**4. Используйте GitHub**
- История коммитов доказывает авторство
- Timestamps показывают когда код был написан
- Публичный репозиторий = публичное доказательство

### Что делать если кто-то украл код?

1. **DMCA Takedown** на GitHub/других платформах
2. **Доказательства**: история коммитов, LICENSE файл
3. **Юрист** (если серьёзный случай)

---

## 🐧 Сборка для Linux

GitHub Actions уже настроен, но для ручной сборки:

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y python3.13 python3-pip portaudio19-dev

# Установить зависимости
pip3 install uv
uv pip install -r requirements.txt
uv pip install pyinstaller

# Собрать
pyinstaller RapidWhisper.spec --clean
```

---

## ❓ FAQ

### Q: Нужен ли мне Mac для сборки под macOS?
**A:** Нет, если используете GitHub Actions. Да, если хотите собирать локально.

### Q: Сколько стоит GitHub Actions?
**A:** Бесплатно для публичных репозиториев. Для приватных: 2000 минут/месяц бесплатно.

### Q: Можно ли собрать macOS на Windows?
**A:** Нет, Apple запрещает это лицензией. Используйте GitHub Actions или облачный Mac.

### Q: Потеряю ли я права если кто-то соберёт проект?
**A:** Нет, компиляция не передаёт авторские права. Вы остаётесь автором.

### Q: Нужна ли подпись для macOS?
**A:** Не обязательно, но рекомендуется. Без подписи пользователи увидят предупреждение.

### Q: Как обновить сборку?
**A:** Просто сделайте `git push` - GitHub Actions пересоберёт автоматически.

### Q: Можно ли собрать ARM версию для M1/M2 Mac?
**A:** Да, GitHub Actions использует M1 runners. Укажите `runs-on: macos-14` для ARM.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в **Actions** → последний запуск
2. Откройте Issue на GitHub
3. Напишите в Discord (если есть)

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Собирать для всех платформ без Mac
- ✅ Автоматизировать релизы
- ✅ Защитить свои авторские права
- ✅ Распространять приложение легально

**Следующий шаг**: Загрузите код на GitHub и запустите первую сборку! 🚀
