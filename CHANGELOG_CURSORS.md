# Changelog: Правильные курсоры для интерактивных элементов

## Дата: 30 января 2026

## Описание изменений

Добавлены правильные курсоры для всех интерактивных элементов в окне настроек, чтобы пользователь понимал, на что можно кликнуть.

## Что изменилось?

### 1. Боковая панель навигации (Sidebar)
- **Курсор**: Рука (PointingHandCursor)
- **Элементы**: 
  - 🤖 AI Provider
  - ⚡ Приложение
  - 🎤 Аудио
  - ℹ️ О программе

### 2. Выпадающие списки (QComboBox)
- **Курсор**: Рука (PointingHandCursor)
- **Элементы**:
  - Выбор провайдера (groq, openai, glm, custom)
  - Частота дискретизации (16000, 44100, 48000)
  - Размер чанка (256, 512, 1024, 2048, 4096)

### 3. Кнопки
- **Курсор**: Рука (PointingHandCursor)
- **Элементы**:
  - Кнопки показа/скрытия пароля (👁)
  - Кнопка "Отмена"
  - Кнопка "💾 Сохранить"

### 4. Поля ввода (QLineEdit, QDoubleSpinBox)
- **Курсор**: Текстовый (IBeam) - по умолчанию
- **Элементы**:
  - API ключи
  - Горячая клавиша
  - Порог тишины
  - Длительность тишины
  - Автоскрытие
  - Custom Base URL
  - Custom Model

## Технические детали

### Реализация

Курсоры установлены программно через метод `setCursor()`:

```python
# Боковая панель
self.sidebar.setCursor(Qt.CursorShape.PointingHandCursor)

# Выпадающий список
self.provider_combo.setCursor(Qt.CursorShape.PointingHandCursor)

# Кнопка
save_btn.setCursor(Qt.CursorShape.PointingHandCursor)
```

### Почему не через CSS?

Qt StyleSheet не поддерживает свойство `cursor`. Попытка использовать:
```css
QComboBox {
    cursor: pointer;  /* Не работает в Qt! */
}
```

Приводит к предупреждениям:
```
Unknown property cursor
```

Поэтому курсоры устанавливаются программно для каждого виджета.

### Типы курсоров в Qt

- **PointingHandCursor** - рука с указательным пальцем (для кликабельных элементов)
- **IBeamCursor** - текстовый курсор (для полей ввода, по умолчанию)
- **ArrowCursor** - обычная стрелка (по умолчанию для большинства элементов)
- **WaitCursor** - песочные часы (для ожидания)
- **CrossCursor** - крестик (для точного выбора)

## Измененные файлы

- **ui/settings_window.py**
  - Добавлен `setCursor()` для sidebar
  - Добавлен `setCursor()` для всех QComboBox
  - Добавлен `setCursor()` для всех QPushButton
  - Удалены неработающие CSS правила для cursor
  - Изменен способ создания QFont (явное указание шрифта)

## Пользовательский опыт

### До изменений:
- ❌ Непонятно, на что можно кликнуть
- ❌ Курсор везде одинаковый (стрелка)
- ❌ Нет визуальной обратной связи

### После изменений:
- ✅ Курсор меняется на "руку" при наведении на кликабельные элементы
- ✅ Понятно, что можно выбрать в выпадающих списках
- ✅ Очевидно, что кнопки можно нажать
- ✅ Текстовый курсор в полях ввода (по умолчанию)

## Совместимость

- ✅ Windows 10/11
- ✅ macOS 10.14+
- ✅ Linux (все дистрибутивы)
- ✅ Все темы оформления
- ✅ Все разрешения экранов

## Дополнительные улучшения

### Исправлено предупреждение QFont
Изменен способ создания шрифтов для заголовков:

**Было:**
```python
title_font = QFont()
title_font.setPointSize(20)
```

**Стало:**
```python
title_font = QFont("Segoe UI", 20)  # Явно указываем шрифт и размер
```

Это устраняет предупреждение:
```
QFont::setPointSize: Point size <= 0 (-1), must be greater than 0
```

## Тестирование

Для проверки изменений:
```bash
uv run python test_about_section.py
```

Проверьте:
1. Наведите курсор на элементы боковой панели - должна появиться "рука"
2. Наведите на выпадающие списки - должна появиться "рука"
3. Наведите на кнопки - должна появиться "рука"
4. Кликните в поля ввода - должен появиться текстовый курсор
5. Никаких предупреждений в консоли

## Следующие шаги

Если нужно добавить курсоры для других элементов:

```python
# Для любого виджета
widget.setCursor(Qt.CursorShape.PointingHandCursor)

# Для временного изменения курсора
QApplication.setOverrideCursor(Qt.CursorShape.WaitCursor)
# ... выполнение операции ...
QApplication.restoreOverrideCursor()
```

---

**Версия:** 1.3.0  
**Статус:** ✅ Готово к использованию
