# Changelog v1.2.0 - AppData Settings & First-Run Setup

## 🎉 Основные изменения

### Настройки в AppData
- ✅ Настройки теперь сохраняются в `%APPDATA%\RapidWhisper\.env` (Windows)
- ✅ Для macOS: `~/Library/Application Support/RapidWhisper/.env`
- ✅ Для Linux: `~/.config/RapidWhisper/.env`
- ✅ Автоматическое создание директории и файла настроек при первом запуске

### Первоначальная настройка
- ✅ Приветственное окно при первом запуске с инструкциями
- ✅ Автоматическое открытие настроек если нет API ключа
- ✅ Уведомления в трее о необходимости установить ключ
- ✅ Блокировка записи до установки API ключа
- ✅ Кликабельные ссылки для получения API ключей

### Сборка и распространение
- ✅ Убран `.env.example` из сборки
- ✅ Только чистый `.exe` файл для распространения
- ✅ Никаких API ключей в собранном файле
- ✅ Обновлен `build.bat` для упрощенной сборки
- ✅ Обновлены инструкции для пользователей

### Улучшения UI
- ✅ Настройки применяются без перезапуска (hot reload)
- ✅ Сохранение настроек в правильную директорию через GUI
- ✅ Проверка наличия API ключа перед записью
- ✅ Информативные сообщения об ошибках

## 📝 Технические детали

### Новые функции в `core/config.py`
- `get_config_dir()` - возвращает путь к директории конфигурации
- `get_env_path()` - возвращает путь к .env (dev или production)
- `create_default_env()` - создает .env с настройками по умолчанию
- `Config.has_api_key()` - проверяет наличие API ключа

### Изменения в `main.py`
- Проверка наличия API ключа при инициализации
- Флаг `_needs_setup` для первоначальной настройки
- Метод `_show_welcome_dialog()` для приветственного окна
- Блокировка записи если нет API ключа
- Автоматическое открытие настроек при первом запуске

### Изменения в `ui/settings_window.py`
- Сохранение в правильную директорию через `get_env_path()`
- Обновленный обработчик сохранения настроек

### Изменения в сборке
- `RapidWhisper.spec` - убраны все .env файлы из datas
- `build.bat` - упрощенный процесс сборки
- Только .exe файл в результате

## 📚 Документация

### Новые файлы
- `docs/APPDATA_SETTINGS.md` - подробное описание работы с настройками
- `CHANGELOG_v1.2.0.md` - этот файл

### Обновленные файлы
- `INSTALL.txt` - новые инструкции для пользователей
- `BUILD_QUICK.md` - обновленная инструкция по сборке
- `DISTRIBUTION_CHECKLIST.md` - обновленный чеклист
- `README.md` - обновленная документация

## 🔄 Миграция с v1.1.0

Если у вас была версия 1.1.0:

1. Ваш старый `.env` остается в папке проекта (для разработки)
2. При запуске собранного .exe создастся новый `.env` в AppData
3. Нужно будет ввести API ключ заново через настройки
4. Или скопировать старый `.env` в `%APPDATA%\RapidWhisper\.env`

## ⚠️ Breaking Changes

- `.env` файл больше не включается в сборку
- Пользователи должны настроить API ключ при первом запуске
- Настройки теперь в AppData, а не в папке с приложением

## 🐛 Исправленные баги

- Исправлена проблема с утечкой API ключей в сборке
- Улучшена безопасность хранения настроек
- Исправлена проблема с потерей настроек при обновлении

## 🎯 Для разработчиков

### Режим разработки
- Если `.env` есть в текущей директории - используется он
- Удобно для разработки и тестирования

### Режим production
- Если `.env` нет в текущей директории - используется AppData
- Автоматически для собранного .exe

### Тестирование первого запуска
```bash
# Удалить настройки
rmdir /s /q %APPDATA%\RapidWhisper

# Запустить приложение
uv run python main.py
```

## 📦 Сборка

```bash
# Простая сборка
build.bat

# Результат
dist\RapidWhisper.exe
```

Только один файл для распространения!

## ✅ Проверено

- [x] Первый запуск показывает приветственное окно
- [x] Настройки сохраняются в AppData
- [x] API ключи не попадают в .exe
- [x] Блокировка записи без ключа работает
- [x] Уведомления в трее работают
- [x] Hot reload настроек работает
- [x] Все тесты проходят (259 tests)

---

**Версия**: 1.2.0  
**Дата**: 30 января 2026  
**Статус**: Готово к релизу 🚀
