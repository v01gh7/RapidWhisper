# Инструкция: HTML Форматирование

## Что изменилось?

Теперь форматирование работает **по-разному** для разных приложений:

### ✅ Word, Google Docs, LibreOffice
- **Формат**: HTML (реальное форматирование!)
- **Результат**: Заголовки становятся РЕАЛЬНЫМИ заголовками, жирный текст - ЖИРНЫМ, списки - СПИСКАМИ
- **Как**: HTML вставляется в буфер обмена, приложение автоматически применяет форматирование

### ✅ Notion, Obsidian, Markdown файлы
- **Формат**: Markdown символы (`# ## ***`)
- **Результат**: Текст с markdown символами, которые эти приложения понимают

## Что нужно сделать?

### 1. Настроить промпты (УЖЕ СДЕЛАНО)
```bash
python setup_html_prompts.py
```

### 2. Перезапустить приложение
Закройте и откройте RapidWhisper заново.

### 3. Протестировать

#### Google Docs:
1. Откройте Google Docs в браузере
2. Создайте новый документ
3. Запишите голосовое сообщение в RapidWhisper
4. Вставьте текст (Ctrl+V)
5. **Проверьте**: Заголовки должны быть крупными, жирный текст - жирным!

#### Word/LibreOffice:
То же самое - откройте документ, запишите, вставьте.

#### Notion/Obsidian:
Должны появиться markdown символы (# для заголовков, ** для жирного).

## Где хранятся промпты?

**ВАЖНО**: Все промпты хранятся ТОЛЬКО в `.env` файле!

- Нет хардкоженных промптов в коде
- Можете редактировать в UI настроек приложения
- Изменения сохраняются в `.env`
- Один источник правды - `.env` файл

## Что делать, если не работает?

### Проблема 1: Текст вставляется без форматирования
**Решение**: Проверьте логи (rapidwhisper.log):
```
✅ Определен формат: word
Тип вывода: html
📋 Вставка HTML в буфер обмена
✅ HTML успешно скопирован
```

Если видите эти сообщения, но форматирование не применяется:
- Попробуйте вставить в другое место документа
- Проверьте, что используете Ctrl+V (не правую кнопку мыши)

### Проблема 2: Google Docs не определяется
**Решение**: Проверьте заголовок вкладки браузера - должно быть "Google Docs" или "Google Документы".

### Проблема 3: Промпт пустой
**Решение**: Запустите `python setup_html_prompts.py` еще раз.

## Технические детали

### Как это работает?

1. **Определение приложения**: RapidWhisper смотрит, какое окно активно
2. **Загрузка промпта из .env**: Читается `FORMATTING_APP_PROMPTS` JSON
3. **Форматирование**: AI получает промпт из .env для этого приложения
4. **Вставка**: 
   - HTML → вставляется через `win32clipboard` с форматированием
   - Markdown → обычный текст через `pyperclip`

### Файлы, которые изменились:

- `services/formatting_config.py` - убраны хардкоженные промпты, все из .env
- `services/rich_clipboard_manager.py` - новый класс для HTML
- `services/formatting_module.py` - логика выбора формата
- `setup_html_prompts.py` - скрипт настройки промптов в .env

## Вопросы?

**Q: Можно ли настроить промпты?**
A: Да! Редактируйте в UI настроек приложения или напрямую в `.env` файле.

**Q: Где хранятся промпты?**
A: ТОЛЬКО в `.env` файле, в `FORMATTING_APP_PROMPTS` JSON.

**Q: Работает ли на Mac/Linux?**
A: Пока только Windows (используется `win32clipboard`).

**Q: Можно ли вернуть старое поведение?**
A: Да, отредактируйте промпты в настройках приложения или в `.env`.
