# Быстрое исправление: Позиция окна не применяется

## Проблема

После сохранения настроек с выбором "Правый верхний угол", окно записи все еще появляется по центру.

## Причина

Была ошибка в логике метода `_show_window_with_config()`. Он проверял `remember_window_position` и передавал `use_saved_position=False`, что приводило к игнорированию preset.

## Исправление

### Старая логика (неправильная):
```python
def _show_window_with_config(self) -> None:
    use_saved = self.config.remember_window_position  # False
    self.floating_window.show_at_center(use_saved_position=use_saved)  # False!
```

**Проблема:** Если `remember_window_position=false`, то `use_saved_position=False`, и preset игнорируется.

### Новая логика (правильная):
```python
def _show_window_with_config(self) -> None:
    # Всегда используем сохраненную позицию (preset или custom)
    self.floating_window.show_at_center(use_saved_position=True)
```

**Решение:** Всегда передаем `True`, а внутри `show_at_center()` проверяется `WINDOW_POSITION_PRESET`:
- Если `preset='custom'` → используется пользовательская позиция (X, Y)
- Если `preset='top_right'` → используется правый верхний угол
- Если `preset='center'` → используется центр

## Как проверить исправление?

### Шаг 1: Перезапустите приложение

**ВАЖНО:** Закройте приложение полностью и запустите снова:

```bash
# Закрыть через трей: Правый клик → Выход
# Или через диспетчер задач

# Запустить снова
uv run python main.py
```

### Шаг 2: Проверьте настройки

1. Откройте настройки
2. Перейдите в "⚡ Приложение" → "Интерфейс"
3. Проверьте:
   - ❌ Галочка "Запоминать позицию окна" снята
   - ✅ Выбрано "Правый верхний угол"

### Шаг 3: Проверьте окно записи

1. Нажмите Ctrl+Space
2. Окно должно появиться в правом верхнем углу! ✅

### Шаг 4: Проверьте логи (опционально)

Откройте файл `rapidwhisper.log` и найдите строку:
```
Показ окна: preset=top_right, remember=false
```

Это подтверждает, что настройки применяются правильно.

## Почему нужен перезапуск?

Хотя мы добавили `load_dotenv(override=True)` для перезагрузки переменных окружения, некоторые компоненты могут кэшировать значения. Перезапуск гарантирует, что все компоненты используют новые настройки.

## Проверка .env файла

Ваш `.env` файл должен содержать:

```env
REMEMBER_WINDOW_POSITION=false
WINDOW_POSITION_PRESET=top_right
```

Проверьте, что эти строки есть в файле:
```bash
# Windows
type .env | findstr WINDOW_POSITION

# Или откройте .env в блокноте
```

## Тестирование всех позиций

После перезапуска проверьте каждую позицию:

| Позиция | Ожидаемое место |
|---------|-----------------|
| Центр | По центру экрана |
| Левый верхний угол | Левый верхний угол с отступом 20px |
| Правый верхний угол | Правый верхний угол с отступом 20px |
| Левый нижний угол | Левый нижний угол с отступом 20px |
| Правый нижний угол | Правый нижний угол с отступом 20px |

## Если все еще не работает

### 1. Проверьте логи

Откройте `rapidwhisper.log` и найдите строки с "Показ окна":
```
Показ окна: preset=top_right, remember=false
```

Если preset неправильный, значит .env не сохранился.

### 2. Проверьте .env файл вручную

Откройте `.env` в текстовом редакторе и убедитесь, что там есть:
```env
REMEMBER_WINDOW_POSITION=false
WINDOW_POSITION_PRESET=top_right
```

### 3. Попробуйте изменить вручную

Если настройки не сохраняются через UI:
1. Закройте приложение
2. Откройте `.env` в блокноте
3. Найдите или добавьте строки:
   ```env
   REMEMBER_WINDOW_POSITION=false
   WINDOW_POSITION_PRESET=top_right
   ```
4. Сохраните файл
5. Запустите приложение

### 4. Проверьте путь к .env

Приложение может использовать .env из разных мест:
- **Разработка**: `.env` в корне проекта
- **Production**: `%APPDATA%/RapidWhisper/.env`

Проверьте оба файла!

## Измененные файлы

### main.py
- Исправлен метод `_show_window_with_config()`
- Теперь всегда передает `use_saved_position=True`

### ui/floating_window.py
- Добавлено логирование preset для отладки

## Связанные документы

- Предустановленные позиции (см. CHANGELOG_WINDOW_PRESETS.md)
- Исправление применения позиций (см. FIX_WINDOW_PRESETS.md)
- Центрирование окна настроек (см. FIX_SETTINGS_WINDOW_CENTER.md)

---

**Версия:** 1.3.0  
**Статус:** ✅ Исправлено  
**Действие:** 🔄 Требуется перезапуск приложения
