# Changelog - Custom Providers Support

## 🎉 Новая функция: Поддержка кастомных AI провайдеров

### Что добавлено

#### Кастомный провайдер
- ✅ Поддержка любых OpenAI-совместимых API
- ✅ Настройка через UI (API ключ, Base URL, Model)
- ✅ Поддержка локальных моделей (LM Studio, Ollama, vLLM)
- ✅ Поддержка удаленных серверов
- ✅ Полная приватность с локальными моделями

#### Обновленный UI
- ✅ Новый провайдер "custom" в выпадающем списке
- ✅ Поля для настройки:
  - Custom API Key
  - Custom Base URL
  - Custom Model
- ✅ Подсветка активных полей при выборе провайдера
- ✅ Ссылки на популярные локальные решения

#### Обновленная конфигурация
- ✅ Новые параметры в Config:
  - `custom_api_key`
  - `custom_base_url`
  - `custom_model`
- ✅ Валидация кастомных настроек
- ✅ Проверка наличия обязательных полей

#### Обновленный TranscriptionClient
- ✅ Поддержка кастомных параметров
- ✅ Передача base_url и model в OpenAI SDK
- ✅ Обработка ошибок для кастомных API

### Технические детали

#### Изменения в `services/transcription_client.py`
```python
def __init__(self, provider: str = "openai", api_key: Optional[str] = None, 
             base_url: Optional[str] = None, model: Optional[str] = None):
    # Поддержка кастомных параметров
    if self.provider == "custom":
        self.base_url = base_url or os.getenv("CUSTOM_BASE_URL")
        self.model = model or os.getenv("CUSTOM_MODEL", "whisper-1")
```

#### Изменения в `core/config.py`
```python
# Новые атрибуты
self.custom_api_key: str = ""
self.custom_base_url: str = ""
self.custom_model: str = "whisper-1"

# Валидация
if self.ai_provider == "custom":
    if not self.custom_api_key:
        errors.append("CUSTOM_API_KEY не найден")
    if not self.custom_base_url:
        errors.append("CUSTOM_BASE_URL не найден")
```

#### Изменения в `ui/settings_window.py`
```python
# Новые поля
self.custom_key_edit = QLineEdit()
self.custom_url_edit = QLineEdit()
self.custom_model_edit = QLineEdit()

# Сохранение
"CUSTOM_API_KEY": self.custom_key_edit.text(),
"CUSTOM_BASE_URL": self.custom_url_edit.text(),
"CUSTOM_MODEL": self.custom_model_edit.text(),
```

#### Изменения в `main.py`
```python
# Передача кастомных параметров
self.transcription_thread = TranscriptionThread(
    self._audio_file_path,
    provider=self.config.ai_provider,
    api_key=self._get_api_key_for_provider(),
    base_url=self.config.custom_base_url if self.config.ai_provider == "custom" else None,
    model=self.config.custom_model if self.config.ai_provider == "custom" else None
)
```

### Примеры использования

#### LM Studio (локально)
```env
AI_PROVIDER=custom
CUSTOM_API_KEY=lm-studio
CUSTOM_BASE_URL=http://localhost:1234/v1/
CUSTOM_MODEL=whisper-1
```

#### Ollama (локально)
```env
AI_PROVIDER=custom
CUSTOM_API_KEY=ollama
CUSTOM_BASE_URL=http://localhost:11434/v1/
CUSTOM_MODEL=whisper
```

#### Удаленный сервер
```env
AI_PROVIDER=custom
CUSTOM_API_KEY=your_secret_key
CUSTOM_BASE_URL=https://your-server.com/v1/
CUSTOM_MODEL=whisper-large-v3
```

### Документация

#### Новые файлы
- `docs/CUSTOM_PROVIDERS.md` - Полное руководство по кастомным провайдерам
- `CHANGELOG_CUSTOM_PROVIDERS.md` - Этот файл

#### Обновленные файлы
- `.env.example` - Добавлены примеры для custom провайдера
- `core/config.py` - Обновлен default .env template
- `README.md` - Упоминание о поддержке кастомных API

### Преимущества

#### Для пользователей
- 🔒 **Приватность**: Локальные модели не отправляют данные в интернет
- 💰 **Экономия**: Бесплатные локальные модели
- ⚡ **Гибкость**: Выбор любого совместимого API
- 🌐 **Офлайн**: Работа без интернета с локальными моделями

#### Для разработчиков
- 🔧 **Расширяемость**: Легко добавить новые провайдеры
- 🧪 **Тестирование**: Локальные модели для разработки
- 🏢 **Enterprise**: Использование корпоративных API

### Совместимость

#### Поддерживаемые API
- ✅ OpenAI API
- ✅ Groq API
- ✅ GLM API
- ✅ LM Studio
- ✅ Ollama
- ✅ vLLM
- ✅ LocalAI
- ✅ FastWhisper
- ✅ Любые OpenAI-совместимые API

#### Требования
- API должен поддерживать endpoint `/audio/transcriptions`
- API должен принимать файлы в формате multipart/form-data
- API должен возвращать JSON с полем `text`

### Тестирование

#### Проверено
- [x] Groq API работает
- [x] OpenAI API работает
- [x] GLM API работает
- [x] LM Studio работает (локально)
- [x] Ollama работает (локально)
- [x] Валидация настроек работает
- [x] UI обновляется корректно
- [x] Сохранение настроек работает
- [x] Hot reload работает

#### Не протестировано
- [ ] vLLM (требует настройку)
- [ ] LocalAI (требует настройку)
- [ ] Удаленные серверы (требует сервер)

### Известные ограничения

1. **API ключ**: Некоторые локальные API не требуют ключ, но поле обязательно (можно указать любое значение)
2. **URL формат**: URL должен заканчиваться на `/v1/`
3. **Модель**: Название модели должно совпадать с тем, что ожидает API

### Миграция

Существующие пользователи:
- ✅ Никаких изменений не требуется
- ✅ Старые провайдеры работают как раньше
- ✅ Можно добавить custom провайдер в любое время

### Roadmap

Планы на будущее:
- [ ] Автоопределение локальных серверов
- [ ] Профили для быстрого переключения
- [ ] Тестирование подключения в UI
- [ ] Поддержка других форматов API (не только OpenAI)

### Благодарности

Спасибо сообществу за запросы на поддержку локальных моделей!

---

**Версия**: 1.3.0  
**Дата**: 30 января 2026  
**Статус**: Готово к тестированию 🧪
