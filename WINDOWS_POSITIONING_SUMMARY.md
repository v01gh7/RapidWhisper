# Сводка: Позиционирование окон в RapidWhisper

## Обзор

В RapidWhisper есть несколько типов окон с разным поведением позиционирования.

## Типы окон

### 1. Окна, которые ВСЕГДА по центру экрана

Эти окна игнорируют настройки позиции и всегда показываются по центру:

#### 1.1. Окно запуска
- **Когда**: При запуске приложения (если есть API ключ)
- **Текст**: "RapidWhisper загружен!"
- **Длительность**: 2 секунды
- **Код**: `show_at_center(use_saved_position=False)`
- **Файл**: `main.py`, метод `run()`

#### 1.2. Окно настроек
- **Когда**: Трей → Настройки
- **Тип**: QDialog
- **Код**: `center_on_screen()`
- **Файл**: `ui/settings_window.py`

#### 1.3. Окно "О программе"
- **Когда**: Трей → О программе
- **Тип**: QMessageBox
- **Код**: Вручную центрируется через `QApplication.primaryScreen()`
- **Файл**: `ui/tray_icon.py`, метод `_show_about()`

#### 1.4. Приветственное окно
- **Когда**: Первый запуск (нет API ключа)
- **Тип**: QMessageBox
- **Код**: Автоматически центрируется (без parent)
- **Файл**: `main.py`, метод `_show_welcome_dialog()`

### 2. Окна, которые используют настройки позиции

Эти окна используют настройки из .env файла:

#### 2.1. Окно записи
- **Когда**: Нажатие горячей клавиши (Ctrl+Space)
- **Настройки**: 
  - `WINDOW_POSITION_PRESET` (center, top_left, top_right, bottom_left, bottom_right, custom)
  - `REMEMBER_WINDOW_POSITION` (true/false)
  - `WINDOW_POSITION_X`, `WINDOW_POSITION_Y` (для custom)
- **Код**: `show_at_center(use_saved_position=True)`
- **Файл**: `main.py`, метод `_show_window_with_config()`

## Настройки позиции окна записи

### Параметры в .env

```env
# Запоминать позицию окна (true/false)
REMEMBER_WINDOW_POSITION=false

# Предустановленная позиция (center, top_left, top_right, bottom_left, bottom_right, custom)
WINDOW_POSITION_PRESET=center

# Пользовательская позиция (только для preset=custom)
WINDOW_POSITION_X=1493
WINDOW_POSITION_Y=39
```

### Варианты позиций

| Preset | Описание | Позиция |
|--------|----------|---------|
| `center` | Центр экрана | По центру |
| `top_left` | Левый верхний угол | Отступ 20px от краев |
| `top_right` | Правый верхний угол | Отступ 20px от краев |
| `bottom_left` | Левый нижний угол | Отступ 20px от краев |
| `bottom_right` | Правый нижний угол | Отступ 20px от краев |
| `custom` | Пользовательская | Из WINDOW_POSITION_X/Y |

### Логика выбора позиции

```
Если REMEMBER_WINDOW_POSITION = true:
    ├─ Preset автоматически = custom
    └─ Используется WINDOW_POSITION_X/Y (сохраняется при перетаскивании)

Если REMEMBER_WINDOW_POSITION = false:
    ├─ Используется WINDOW_POSITION_PRESET
    └─ Если preset = custom → используется WINDOW_POSITION_X/Y
        Иначе → используется предустановленная позиция
```

## Методы позиционирования

### 1. `show_at_center(use_saved_position: bool = True)`

**Файл**: `ui/floating_window.py`

**Параметры**:
- `use_saved_position=True`: Использует настройки из .env
- `use_saved_position=False`: Всегда по центру

**Логика**:
```python
if use_saved_position:
    preset = os.getenv('WINDOW_POSITION_PRESET', 'center')
    
    if preset == 'custom':
        x, y = load_position()  # Из WINDOW_POSITION_X/Y
    elif preset == 'center':
        x, y = screen_center
    elif preset == 'top_right':
        x, y = screen_top_right
    # и т.д.
else:
    x, y = screen_center  # ВСЕГДА центр
```

### 2. `center_on_screen()`

**Файл**: `ui/settings_window.py`

**Назначение**: Центрирует окно настроек

**Логика**:
```python
screen_geometry = QApplication.primaryScreen().availableGeometry()
x = screen_center_x - window_width // 2
y = screen_center_y - window_height // 2
self.move(x, y)
```

### 3. Ручное центрирование (QMessageBox)

**Файл**: `ui/tray_icon.py`

**Назначение**: Центрирует окно "О программе"

**Логика**:
```python
msg = QMessageBox()  # БЕЗ parent
screen_geometry = QApplication.primaryScreen().availableGeometry()
msg_size = msg.sizeHint()
x = screen_center_x - msg_size.width() // 2
y = screen_center_y - msg_size.height() // 2
msg.move(x, y)
```

## Таблица: Все окна и их позиционирование

| Окно | Тип | Позиция | Настройки | Метод |
|------|-----|---------|-----------|-------|
| Окно запуска | FloatingWindow | Всегда центр | Игнорирует | `show_at_center(False)` |
| Окно записи | FloatingWindow | Из настроек | Использует | `show_at_center(True)` |
| Окно настроек | QDialog | Всегда центр | Игнорирует | `center_on_screen()` |
| О программе | QMessageBox | Всегда центр | Игнорирует | Ручное центрирование |
| Приветствие | QMessageBox | Всегда центр | Игнорирует | Автоматически (без parent) |

## Примеры использования

### Пример 1: Окно записи в правом верхнем углу

**.env:**
```env
REMEMBER_WINDOW_POSITION=false
WINDOW_POSITION_PRESET=top_right
```

**Результат**:
- Окно запуска: **центр** ✅
- Окно записи: **правый верхний угол** ✅
- Окно настроек: **центр** ✅
- О программе: **центр** ✅

### Пример 2: Запоминание позиции

**.env:**
```env
REMEMBER_WINDOW_POSITION=true
WINDOW_POSITION_X=1500
WINDOW_POSITION_Y=100
WINDOW_POSITION_PRESET=custom
```

**Результат**:
- Окно запуска: **центр** ✅
- Окно записи: **позиция (1500, 100)** ✅
- При перетаскивании: **позиция обновляется** ✅
- Окно настроек: **центр** ✅
- О программе: **центр** ✅

### Пример 3: Центр по умолчанию

**.env:**
```env
REMEMBER_WINDOW_POSITION=false
WINDOW_POSITION_PRESET=center
```

**Результат**:
- Все окна: **центр** ✅

## Преимущества текущей архитектуры

✅ **Гибкость**: Окно записи настраивается, служебные окна - нет  
✅ **Консистентность**: Все служебные окна всегда по центру  
✅ **Интуитивность**: Пользователь не путается, где появится окно  
✅ **Простота**: Один параметр контролирует поведение  

## Файлы

- `main.py` - логика показа окон
- `ui/floating_window.py` - метод `show_at_center()`
- `ui/settings_window.py` - метод `center_on_screen()`
- `ui/tray_icon.py` - метод `_show_about()`
- `.env` - настройки позиции

## Статус

🟢 **Все окна позиционируются корректно**

Служебные окна всегда по центру, окно записи использует настройки.
