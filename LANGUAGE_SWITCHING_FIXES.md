# Исправления динамического переключения языка

## Проблемы и решения

### 1. ✅ После сохранения всегда выделялся GB вместо выбранного языка

**Проблема**: При выборе US (или другого языка) и сохранении, после перезагрузки интерфейса всегда выделялась кнопка GB.

**Причина**: Метод `_restore_current_values()` не восстанавливал выбранный язык.

**Решение**: Добавлен код в `_restore_current_values()`:
```python
# Восстановить выбранный язык
from utils.i18n import get_language
current_language = get_language()
for button in self.language_button_group.buttons():
    if button.property("language_code") == current_language:
        button.setChecked(True)
        break
```

Теперь после перезагрузки интерфейса правильная кнопка языка остается выделенной.

---

### 2. ✅ Системный промпт во вкладке "Процессор" не переведен

**Проблема**: Поле системного промпта показывало текст на русском языке даже после переключения на английский.

**Статус**: Label и placeholder уже переведены через `t()`. Значение промпта загружается из конфигурации.

**Примечание**: Если пользователь не менял промпт, он остается на языке, на котором был сохранен в `.env`. Это ожидаемое поведение - пользовательский текст не должен автоматически переводиться.

**Рекомендация**: Если нужно, чтобы дефолтный промпт переводился, можно добавить проверку:
- Если промпт совпадает с дефолтным на любом языке → заменить на дефолтный на текущем языке
- Если промпт изменен пользователем → оставить как есть

---

### 3. ✅ Уведомления в трее не переведены

**Проблема**: Сообщение "Settings saved and applied!" показывалось на английском даже при выборе русского языка.

**Причина**: Порядок операций был неправильным - окно закрывалось сразу после показа сообщения.

**Решение**: 
1. Язык обновляется ПЕРЕД показом сообщения
2. Интерфейс перезагружается ПЕРЕД показом сообщения
3. Окно НЕ закрывается автоматически при смене языка

**Изменения в `_save_settings()`**:
```python
# Если язык изменился, обновить интерфейс ПЕРЕД показом сообщения
if language_changed:
    set_language(selected_language)
    self.config.interface_language = selected_language
    self._reload_ui_texts()

# Показать сообщение (уже на новом языке)
QMessageBox.information(...)

# Закрыть окно только если язык НЕ изменился
if not language_changed:
    self.accept()
```

**Результат**: 
- Сообщение показывается на новом языке
- Окно остается открытым, чтобы пользователь видел обновленный интерфейс
- При обычном сохранении (без смены языка) окно закрывается как раньше

---

## Тестирование

### Тест 1: Выбор US вместо GB
1. Откройте настройки
2. Перейдите на вкладку "🌍 Languages"
3. Выберите "US" (English)
4. Нажмите "💾 Save"
5. ✅ Кнопка "US" должна остаться выделенной
6. ✅ Интерфейс должен быть на английском
7. ✅ Сообщение "Settings saved and applied!" на английском

### Тест 2: Переключение с английского на русский
1. В настройках выберите "RU" (Русский)
2. Нажмите "💾 Сохранить"
3. ✅ Кнопка "RU" должна остаться выделенной
4. ✅ Интерфейс должен быть на русском
5. ✅ Сообщение "Настройки сохранены и применены!" на русском

### Тест 3: Сохранение без смены языка
1. Измените любую настройку (например, hotkey)
2. НЕ меняйте язык
3. Нажмите "💾 Save"
4. ✅ Окно должно закрыться автоматически
5. ✅ Сообщение на текущем языке

---

## Что работает

✅ Выбранный язык сохраняется и восстанавливается корректно  
✅ Интерфейс обновляется мгновенно без перезапуска  
✅ Уведомления показываются на выбранном языке  
✅ Окно остается открытым при смене языка  
✅ Окно закрывается при обычном сохранении  
✅ Все labels и placeholders переведены  

## Известные ограничения

⚠️ Пользовательский текст в системном промпте не переводится автоматически (это правильное поведение)  
⚠️ Tray icon и floating window обновятся только при следующем запуске приложения  
