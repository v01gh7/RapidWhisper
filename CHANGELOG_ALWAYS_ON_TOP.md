# Changelog: Окно записи всегда поверх всех приложений

## Дата: 30 января 2026

## Описание изменений

Улучшено поведение плавающего окна записи - теперь оно гарантированно остается поверх всех других приложений, даже когда пользователь переключается на другие программы.

## Проблема

**До изменений:**
- Окно записи могло скрываться за другими приложениями
- При переключении на другую программу окно могло потеряться
- Пользователь не видел, что идет запись, работая в других приложениях

## Решение

**После изменений:**
- ✅ Окно всегда остается поверх всех приложений
- ✅ Можно работать в других программах и видеть статус записи
- ✅ Окно не забирает фокус у других приложений
- ✅ Можно одновременно говорить и печатать в другой программе

## Технические детали

### 1. Добавлены флаги окна

```python
self.setWindowFlags(
    Qt.WindowType.FramelessWindowHint |           # Без рамки
    Qt.WindowType.WindowStaysOnTopHint |          # Всегда поверх
    Qt.WindowType.Tool |                          # Tool window (не в таскбаре)
    Qt.WindowType.WindowDoesNotAcceptFocus       # Не забирает фокус
)
```

**Что делает каждый флаг:**

- **FramelessWindowHint** - окно без рамки и заголовка (минималистичный дизайн)
- **WindowStaysOnTopHint** - окно всегда поверх всех других окон
- **Tool** - окно-инструмент (не показывается в панели задач)
- **WindowDoesNotAcceptFocus** - окно не забирает фокус у других приложений

### 2. Добавлены атрибуты окна

```python
# Показывать окно даже когда приложение не в фокусе
self.setAttribute(Qt.WidgetAttribute.WA_ShowWithoutActivating)
```

**WA_ShowWithoutActivating** - окно показывается без активации, не забирая фокус у текущего приложения.

### 3. Улучшен метод показа окна

```python
def show_at_center(self) -> None:
    # ... позиционирование ...
    
    self.show()
    self.raise_()  # Поднять окно наверх
    self.setWindowState(Qt.WindowState.WindowActive)
    
    # Принудительно установить флаг "всегда поверх"
    self.setWindowFlags(...)
    self.show()  # Показать снова после изменения флагов
```

Двойной вызов `show()` необходим, так как изменение флагов окна требует повторного показа.

## Сценарии использования

### Сценарий 1: Работа в текстовом редакторе
1. Пользователь работает в Word/Google Docs
2. Нажимает Ctrl+Space для начала записи
3. Окно записи появляется поверх редактора
4. Пользователь продолжает печатать и одновременно говорит
5. Окно записи остается видимым все время

### Сценарий 2: Работа в браузере
1. Пользователь читает статью в браузере
2. Нажимает Ctrl+Space для записи заметки
3. Окно записи появляется поверх браузера
4. Пользователь может прокручивать страницу и говорить одновременно
5. Окно не мешает работе, но всегда видно

### Сценарий 3: Работа с несколькими окнами
1. Пользователь работает с несколькими приложениями
2. Начинает запись голосом
3. Переключается между приложениями (Alt+Tab)
4. Окно записи остается видимым поверх всех окон
5. Пользователь всегда видит статус записи

## Измененные файлы

- **ui/floating_window.py**
  - Добавлен флаг `WindowDoesNotAcceptFocus`
  - Добавлен атрибут `WA_ShowWithoutActivating`
  - Улучшен метод `show_at_center()` с принудительной установкой флагов
  - Добавлен `setWindowState(Qt.WindowState.WindowActive)`

## Преимущества

### Для пользователя:
- 🎯 Всегда видно, что идет запись
- 🎯 Можно работать в других приложениях
- 🎯 Не нужно переключаться обратно, чтобы увидеть статус
- 🎯 Окно не мешает работе (не забирает фокус)

### Для UX:
- ✨ Минималистичный дизайн не отвлекает
- ✨ Визуальная обратная связь всегда на виду
- ✨ Естественный workflow - говорить и работать одновременно
- ✨ Не нужно запоминать, что запись идет

## Совместимость

- ✅ Windows 10/11 - полная поддержка
- ✅ macOS 10.14+ - полная поддержка
- ✅ Linux (X11) - полная поддержка
- ⚠️ Linux (Wayland) - может требовать дополнительных настроек

### Особенности платформ

**Windows:**
- Работает идеально из коробки
- Окно остается поверх даже полноэкранных приложений

**macOS:**
- Работает с системными настройками по умолчанию
- Может потребоваться разрешение в System Preferences > Security & Privacy

**Linux (X11):**
- Работает стабильно
- Зависит от оконного менеджера (GNOME, KDE, XFCE)

**Linux (Wayland):**
- Может требовать дополнительных разрешений
- Некоторые композиторы могут ограничивать "always on top"

## Тестирование

### Как проверить:

1. Запустите приложение:
```bash
uv run python main.py
```

2. Откройте любое другое приложение (браузер, редактор, и т.д.)

3. Нажмите Ctrl+Space для начала записи

4. Проверьте, что окно записи видно поверх другого приложения

5. Попробуйте:
   - Переключиться на другое приложение (Alt+Tab)
   - Кликнуть в другое окно
   - Открыть новое окно
   - Развернуть окно на весь экран

6. Окно записи должно оставаться видимым во всех случаях

### Ожидаемое поведение:

✅ Окно записи всегда видно  
✅ Окно не забирает фокус  
✅ Можно печатать в других приложениях  
✅ Можно переключаться между окнами  
✅ Окно остается поверх полноэкранных приложений  

## Известные ограничения

1. **Полноэкранные игры**: Некоторые игры в эксклюзивном полноэкранном режиме могут скрывать окно
2. **Презентации**: PowerPoint/Keynote в режиме презентации могут скрывать окно
3. **Виртуальные машины**: Окно может не отображаться внутри VM в полноэкранном режиме

## Обходные пути

Если окно не отображается поверх:

1. **Windows**: Проверьте настройки "Focus Assist" (не должно блокировать)
2. **macOS**: Дайте разрешение в System Preferences > Security & Privacy
3. **Linux**: Проверьте настройки оконного менеджера

## Дополнительные улучшения

### Возможные будущие улучшения:

1. **Настройка позиции**: Позволить пользователю выбрать угол экрана
2. **Прозрачность**: Настраиваемая прозрачность окна
3. **Размер**: Настраиваемый размер окна
4. **Тема**: Светлая/темная тема окна

## Связанные изменения

- Окно настроек также имеет правильные курсоры (см. CHANGELOG_CURSORS.md)
- Окно настроек имеет адаптивную высоту со скроллом (см. CHANGELOG_ABOUT_SECTION.md)

---

**Версия:** 1.3.0  
**Статус:** ✅ Готово к использованию  
**Приоритет:** 🔥 Высокий (критично для UX)
