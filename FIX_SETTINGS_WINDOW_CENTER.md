# Исправление: Окно настроек всегда по центру

## Дата: 30 января 2026

## Проблема

Окно настроек появлялось в той же позиции, что и окно записи. Если пользователь настроил окно записи на появление в углу экрана, окно настроек тоже появлялось в углу, что было неудобно.

## Ожидаемое поведение

- **Окно записи** - должно появляться в настроенной позиции (центр, углы, или пользовательская)
- **Окно настроек** - должно ВСЕГДА появляться по центру экрана, независимо от настроек

## Решение

### 1. Добавлен метод center_on_screen() в SettingsWindow

```python
def center_on_screen(self) -> None:
    """
    Центрирует окно настроек на экране.
    
    Вызывается перед показом окна, чтобы оно всегда появлялось по центру,
    независимо от настроек позиции окна записи.
    """
    # Получить геометрию экрана
    screen = QApplication.primaryScreen()
    if screen:
        screen_geometry = screen.availableGeometry()
        
        # Вычислить центр
        x = screen_geometry.center().x() - self.width() // 2
        y = screen_geometry.center().y() - self.height() // 2
        
        # Переместить окно
        self.move(x, y)
```

### 2. Вызов метода перед показом окна

В `main.py`, метод `_show_settings()`:

```python
def _show_settings(self) -> None:
    """Показывает окно настроек."""
    from ui.settings_window import SettingsWindow
    
    settings_window = SettingsWindow(self.config, parent=self.floating_window)
    settings_window.settings_saved.connect(self._on_settings_saved)
    
    # Центрировать окно настроек на экране
    settings_window.center_on_screen()
    
    settings_window.exec()
```

## Как это работает?

### Последовательность действий:

1. **Пользователь открывает настройки**
   - Через трей: "Настройки"
   - Или при первом запуске без API ключа

2. **Создается окно настроек**
   ```python
   settings_window = SettingsWindow(self.config, parent=self.floating_window)
   ```

3. **Окно центрируется**
   ```python
   settings_window.center_on_screen()
   ```
   - Получается геометрия экрана
   - Вычисляется центр: `x = center.x() - width // 2`
   - Окно перемещается: `self.move(x, y)`

4. **Окно показывается**
   ```python
   settings_window.exec()
   ```

5. **Результат**: Окно настроек всегда по центру! ✅

## Разница между окнами

### Окно записи (FloatingWindow)
```python
def show_at_center(self, use_saved_position: bool = True):
    # Читает настройки из .env
    preset = os.getenv('WINDOW_POSITION_PRESET', 'center')
    
    # Использует предустановленную или пользовательскую позицию
    if preset == 'top_right':
        x = geometry.right() - window_width - margin
        y = geometry.top() + margin
    ...
```

**Поведение:**
- Зависит от настроек пользователя
- Может появляться в углах, по центру, или в пользовательской позиции
- Позиция сохраняется при перетаскивании

### Окно настроек (SettingsWindow)
```python
def center_on_screen(self):
    # ВСЕГДА центрирует окно
    x = screen_geometry.center().x() - self.width() // 2
    y = screen_geometry.center().y() - self.height() // 2
    self.move(x, y)
```

**Поведение:**
- НЕ зависит от настроек
- ВСЕГДА появляется по центру
- Позиция НЕ сохраняется

## Измененные файлы

### 1. main.py
- Обновлен метод `_show_settings()`
- Добавлен вызов `settings_window.center_on_screen()`

### 2. ui/settings_window.py
- Добавлен метод `center_on_screen()`

## Тестирование

### Проверка центрирования окна настроек:

1. **Настройте окно записи в угол:**
   - Откройте настройки
   - Снимите галочку "Запоминать позицию окна"
   - Выберите "Правый верхний угол"
   - Сохраните

2. **Проверьте окно записи:**
   - Нажмите Ctrl+Space
   - Окно записи должно появиться в правом верхнем углу ✅

3. **Проверьте окно настроек:**
   - Откройте настройки снова (через трей)
   - Окно настроек должно появиться по центру ✅

4. **Повторите для других позиций:**
   - Левый верхний угол
   - Левый нижний угол
   - Правый нижний угол
   - Пользовательская позиция

### Ожидаемые результаты:

| Действие | Окно записи | Окно настроек |
|----------|-------------|---------------|
| Настройка: Центр | По центру ✅ | По центру ✅ |
| Настройка: Правый верхний угол | В правом верхнем углу ✅ | По центру ✅ |
| Настройка: Левый нижний угол | В левом нижнем углу ✅ | По центру ✅ |
| Перетащили окно записи | В перетащенной позиции ✅ | По центру ✅ |

## Почему это важно?

### UX проблемы без исправления:

1. **Окно настроек в углу экрана**
   - Неудобно читать и редактировать
   - Может выходить за пределы экрана на маленьких мониторах
   - Не соответствует ожиданиям пользователя

2. **Путаница в настройках**
   - Пользователь думает, что настройка позиции влияет на все окна
   - Непонятно, почему окно настроек появляется в углу

3. **Плохой первый опыт**
   - При первом запуске окно настроек может появиться в неудобном месте
   - Пользователь может не понять, как это исправить

### Преимущества после исправления:

1. **Предсказуемое поведение**
   - Окно настроек всегда по центру
   - Соответствует стандартам UI/UX

2. **Удобство использования**
   - Легко читать и редактировать настройки
   - Окно всегда в удобном месте

3. **Разделение ответственности**
   - Настройка позиции влияет только на окно записи
   - Окно настроек независимо

## Альтернативные подходы

### Подход 1: Сохранять позицию окна настроек отдельно (не использован)
```python
# Сохранять SETTINGS_WINDOW_POSITION_X/Y
```

**Минусы:**
- Усложняет конфигурацию
- Пользователю нужно настраивать две позиции
- Не соответствует стандартам (окна настроек обычно по центру)

### Подход 2: Использовать Qt центрирование (не использован)
```python
settings_window.setGeometry(
    QStyle.alignedRect(
        Qt.LeftToRight,
        Qt.AlignCenter,
        settings_window.size(),
        QApplication.desktop().availableGeometry()
    )
)
```

**Минусы:**
- Более сложный код
- Зависит от Qt API

### Подход 3: Простое центрирование (использован) ✅
```python
def center_on_screen(self):
    x = screen_geometry.center().x() - self.width() // 2
    y = screen_geometry.center().y() - self.height() // 2
    self.move(x, y)
```

**Плюсы:**
- Простой и понятный код
- Работает надежно
- Легко поддерживать

## Совместимость

- ✅ Windows 10/11
- ✅ macOS 10.14+
- ✅ Linux (все дистрибутивы)
- ✅ Мультимониторные конфигурации

### Особенности на разных платформах:

**Windows:**
- Окно центрируется на основном мониторе
- Работает корректно с разными DPI

**macOS:**
- Окно центрируется с учетом menu bar
- Работает корректно в полноэкранном режиме

**Linux:**
- Окно центрируется на основном мониторе
- Работает с разными оконными менеджерами

## Связанные изменения

- Предустановленные позиции окна записи (см. CHANGELOG_WINDOW_PRESETS.md)
- Исправление применения позиций (см. FIX_WINDOW_PRESETS.md)
- Запоминание позиции (см. UPDATE_WINDOW_POSITION.md)

---

**Версия:** 1.3.0  
**Статус:** ✅ Исправлено  
**Приоритет:** 🔥 Высокий (UX проблема)
