# Changelog: Окно запуска всегда по центру

## Дата: 2026-01-30

## Проблема

При первом запуске программы появляется окно "RapidWhisper загружен!" на 2 секунды. Это окно показывалось в позиции, указанной в настройках (например, в правом верхнем углу), а не по центру экрана.

## Решение

Окно запуска теперь всегда показывается по центру экрана, независимо от настроек позиции окна записи.

## Изменения

### Файл: `main.py`

**Было:**
```python
# Показать окно при запуске на 2 секунды
self.floating_window.show_at_center()
```

**Стало:**
```python
# Показать окно при запуске на 2 секунды - ВСЕГДА ПО ЦЕНТРУ
self.floating_window.show_at_center(use_saved_position=False)
```

## Логика

### Параметр `use_saved_position`

- `True` (по умолчанию): Использует настройки позиции из .env
  - Если `WINDOW_POSITION_PRESET=top_right` → окно в правом верхнем углу
  - Если `WINDOW_POSITION_PRESET=custom` → окно в пользовательской позиции
  - Если `WINDOW_POSITION_PRESET=center` → окно по центру

- `False`: Игнорирует настройки, всегда показывает по центру
  - Используется для окна запуска
  - Используется для окна настроек
  - Используется для окна "О программе"

## Поведение

### До исправления:
1. Пользователь установил `WINDOW_POSITION_PRESET=top_right`
2. Запускает приложение
3. Окно "RapidWhisper загружен!" появляется в правом верхнем углу ❌
4. Через 2 секунды скрывается
5. При нажатии Ctrl+Space окно записи появляется в правом верхнем углу ✅

### После исправления:
1. Пользователь установил `WINDOW_POSITION_PRESET=top_right`
2. Запускает приложение
3. Окно "RapidWhisper загружен!" появляется **по центру** ✅
4. Через 2 секунды скрывается
5. При нажатии Ctrl+Space окно записи появляется в правом верхнем углу ✅

## Окна, которые всегда по центру

После всех исправлений, следующие окна всегда показываются по центру:

1. ✅ **Окно запуска** ("RapidWhisper загружен!")
   - Показывается при запуске приложения
   - Автоматически скрывается через 2 секунды
   - `show_at_center(use_saved_position=False)`

2. ✅ **Окно настроек**
   - Открывается через трей → Настройки
   - Центрируется методом `center_on_screen()`
   - Не зависит от настроек позиции

3. ✅ **Окно "О программе"**
   - Открывается через трей → О программе
   - Центрируется вручную через `QApplication.primaryScreen()`
   - Не зависит от настроек позиции

4. ✅ **Приветственное окно** (первый запуск)
   - Показывается при отсутствии API ключа
   - Центрируется автоматически (QMessageBox без parent)
   - Не зависит от настроек позиции

## Окна, которые используют настройки позиции

1. ✅ **Окно записи**
   - Показывается при нажатии горячей клавиши
   - Использует настройки из .env
   - `show_at_center(use_saved_position=True)` (по умолчанию)

## Технические детали

### Метод `show_at_center()`

```python
def show_at_center(self, use_saved_position: bool = True) -> None:
    if use_saved_position:
        # Читаем настройки из .env
        preset = os.getenv('WINDOW_POSITION_PRESET', 'center')
        
        if preset == 'custom':
            # Используем пользовательскую позицию
            x, y = self.load_position()
        elif preset == 'center':
            # Центр экрана
            x = screen_center_x
            y = screen_center_y
        elif preset == 'top_right':
            # Правый верхний угол
            x = screen_right - window_width - margin
            y = screen_top + margin
        # и т.д.
    else:
        # ВСЕГДА центр экрана
        x = screen_center_x
        y = screen_center_y
```

### Вызовы метода

**Окно запуска** (main.py, метод `run()`):
```python
self.floating_window.show_at_center(use_saved_position=False)
```

**Окно записи** (main.py, метод `_show_window_with_config()`):
```python
self.floating_window.show_at_center(use_saved_position=True)
```

## Преимущества

✅ **Консистентность**: Все служебные окна по центру  
✅ **Интуитивность**: Окно запуска не "прыгает" в угол  
✅ **Гибкость**: Окно записи все еще использует настройки  
✅ **Простота**: Один параметр контролирует поведение  

## Файлы изменены

- ✅ `main.py` - добавлен параметр `use_saved_position=False`
- ✅ `CHANGELOG_STARTUP_WINDOW_CENTER.md` - эта документация

## Статус

🟢 **Готово к использованию**

Окно запуска теперь всегда показывается по центру экрана.
