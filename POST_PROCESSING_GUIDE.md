# Руководство по постобработке транскрипций

## Обзор

Постобработка транскрипций позволяет автоматически улучшать качество распознанного текста:
- Исправление грамматических ошибок
- Добавление знаков препинания
- Улучшение структуры текста
- Сохранение оригинального смысла

## Доступные провайдеры

### 1. Groq (Рекомендуется) ✅

**Преимущества:**
- Бесплатный
- Очень быстрый
- Высокое качество

**Модели:**
- `llama-3.3-70b-versatile` (рекомендуется)
- `llama-3.1-70b-versatile`
- `mixtral-8x7b-32768`

**Настройка:**
1. Получите API ключ: https://console.groq.com/keys
2. Добавьте в `.env`: `GROQ_API_KEY=your_key`
3. В настройках выберите провайдер `groq`

---

### 2. OpenAI ⚠️

**Преимущества:**
- Высокое качество
- Стабильный API

**Недостатки:**
- Платный

**Модели:**
- `gpt-4o` (лучшее качество)
- `gpt-4o-mini` (быстрее и дешевле)
- `gpt-4-turbo`

**Настройка:**
1. Получите API ключ: https://platform.openai.com/api-keys
2. Добавьте в `.env`: `OPENAI_API_KEY=your_key`
3. В настройках выберите провайдер `openai`

---

### 3. GLM (Zhipu AI) ⚠️

**Два варианта использования:**

#### A. Обычный API (требует баланс для chat)

**Модели:**
- `glm-4-plus` (флагманская)
- `glm-4-0520` (стабильная)
- `glm-4-air` (облегченная)
- `glm-4-airx` (еще легче)
- `glm-4-flash` (самая быстрая)

**Настройка:**
1. Получите API ключ: https://open.bigmodel.cn/usercenter/apikeys
2. Добавьте в `.env`: `GLM_API_KEY=your_key`
3. В настройках выберите провайдер `glm`
4. **НЕ включайте** чекбокс "Использовать Coding Plan"

#### B. Coding Plan (для подписчиков)

**Модели:**
- `glm-4.7` (последняя версия)
- `glm-4.6` (длинный контекст)
- `glm-4.5` (355B параметров)
- `glm-4.5-air` (106B параметров)

**Настройка:**
1. Убедитесь что у вас есть подписка Coding Plan
2. Используйте тот же API ключ
3. В настройках выберите провайдер `glm`
4. **Включите** чекбокс "Использовать Coding Plan подписку"

**⚠️ Важно:** Если запрос зависает, отключите чекбокс Coding Plan

---

### 4. LLM (Локальные модели) 🖥️

**Преимущества:**
- Полная приватность
- Бесплатно
- Работает оффлайн

**Недостатки:**
- Требует мощное железо
- Медленнее облачных API

**Поддерживаемые платформы:**
- LM Studio
- Ollama
- vLLM
- LocalAI
- Любые OpenAI-совместимые локальные серверы

**Настройка:**

1. **Установите локальный сервер:**
   - LM Studio: https://lmstudio.ai
   - Ollama: https://ollama.ai

2. **Запустите сервер:**
   - LM Studio: запустите локальный сервер в интерфейсе
   - Ollama: `ollama serve`

3. **Настройте RapidWhisper:**
   - В настройках выберите провайдер `llm`
   - Укажите Base URL (например: `http://localhost:1234/v1/`)
   - Укажите API ключ (обычно `local` или любой)
   - Выберите модель или введите название

**Рекомендуемые модели:**
- `llama-3.3-70b-versatile`
- `mistral-7b-instruct`
- `mixtral-8x7b-instruct`
- `qwen-2.5-72b-instruct`

---

## Настройка системного промпта

Вы можете изменить системный промпт для настройки поведения модели:

**Промпт по умолчанию:**
```
Ты - редактор текста. Твоя задача: исправить грамматические ошибки, 
добавить знаки препинания и улучшить читаемость текста. 
Сохрани оригинальный смысл и стиль. Не добавляй ничего лишнего. 
Верни только исправленный текст без комментариев.
```

**Примеры кастомных промптов:**

1. **Для технических текстов:**
```
Ты - редактор технической документации. Исправь ошибки, добавь пунктуацию, 
сохрани технические термины без изменений. Верни только исправленный текст.
```

2. **Для деловой переписки:**
```
Ты - редактор деловых писем. Исправь ошибки, добавь пунктуацию, 
сделай текст более формальным и профессиональным. Верни только исправленный текст.
```

3. **Для разговорной речи:**
```
Ты - редактор разговорной речи. Исправь только явные ошибки, 
добавь пунктуацию, сохрани разговорный стиль. Верни только исправленный текст.
```

---

## Тестирование

### Тест всех провайдеров:
```bash
uv run python test_post_processing_providers.py
```

### Тест GLM Coding Plan:
```bash
uv run python test_glm_coding_plan.py
```

---

## Устранение неполадок

### Запрос зависает

**Решение:**
- ✅ **Автоматическая защита**: Запрос автоматически прервется через 60 секунд
- ✅ **Гарантия**: При таймауте вернется оригинальный текст без обработки
- ✅ **Безопасность**: Приложение не зависнет, транскрипция будет сохранена

**Если часто происходит таймаут:**
1. Попробуйте другой провайдер (Groq - самый быстрый)
2. Для GLM: отключите чекбокс Coding Plan
3. Проверьте интернет-соединение
4. Используйте более быструю модель

### Ошибка аутентификации

**Причины:**
1. Неверный API ключ
2. Ключ не активирован
3. Недостаточно средств на балансе

**Решение:**
1. Проверьте API ключ в `.env`
2. Убедитесь что ключ активен
3. Проверьте баланс аккаунта

### Низкое качество обработки

**Решение:**
1. Попробуйте другую модель
2. Измените системный промпт
3. Используйте более мощную модель (gpt-4o, glm-4.7)

---

## Рекомендации

1. **Для начала:** Используйте Groq (бесплатный и быстрый)
2. **Для качества:** Используйте OpenAI gpt-4o (платный)
3. **Для приватности:** Используйте локальные LLM модели
4. **Для GLM:** Если есть Coding Plan, включите чекбокс

---

## Параметры в .env

```env
# Включить постобработку
ENABLE_POST_PROCESSING=true

# Провайдер (groq, openai, glm, llm)
POST_PROCESSING_PROVIDER=groq

# Модель
POST_PROCESSING_MODEL=llama-3.3-70b-versatile

# Системный промпт
POST_PROCESSING_PROMPT=Ты - редактор текста...

# GLM Coding Plan (только для GLM)
GLM_USE_CODING_PLAN=false

# LLM настройки (только для локальных моделей)
LLM_BASE_URL=http://localhost:1234/v1/
LLM_API_KEY=local
```
