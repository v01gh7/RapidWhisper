# Тестирование первого запуска

## Подготовка

### 1. Удалить существующие настройки

```bash
# Windows
rmdir /s /q %APPDATA%\RapidWhisper

# macOS
rm -rf ~/Library/Application\ Support/RapidWhisper

# Linux
rm -rf ~/.config/RapidWhisper
```

### 2. Запустить приложение

```bash
# Для разработки (удалить .env в текущей директории)
del .env
uv run python main.py

# Или запустить собранный .exe
dist\RapidWhisper.exe
```

## Ожидаемое поведение

### При первом запуске

1. ✅ Создается директория `%APPDATA%\RapidWhisper`
2. ✅ Создается файл `.env` с настройками по умолчанию
3. ✅ Показывается приветственное окно с инструкциями
4. ✅ В окне есть ссылки на получение API ключей
5. ✅ Показывается уведомление в трее
6. ✅ Автоматически открывается окно настроек
7. ✅ Статус в трее: "⚠️ Требуется настройка API ключа"

### При попытке записи без ключа

1. ✅ Нажатие Ctrl+Space НЕ начинает запись
2. ✅ Показывается уведомление: "⚠️ API ключ не установлен"
3. ✅ Автоматически открывается окно настроек

### После установки ключа

1. ✅ Настройки сохраняются в `%APPDATA%\RapidWhisper\.env`
2. ✅ Показывается уведомление: "✅ Настройки обновлены"
3. ✅ Статус в трее: "Готово! Нажмите Ctrl+Space для записи"
4. ✅ Запись работает нормально

### При повторном запуске

1. ✅ Настройки загружаются из AppData
2. ✅ Приветственное окно НЕ показывается
3. ✅ Приложение готово к работе сразу
4. ✅ Окно показывается на 2 секунды с "RapidWhisper загружен!"

## Проверка файлов

### Проверить что .env создан

```bash
# Windows
type %APPDATA%\RapidWhisper\.env

# macOS/Linux
cat ~/Library/Application\ Support/RapidWhisper/.env
# или
cat ~/.config/RapidWhisper/.env
```

Должен содержать:
- Все настройки по умолчанию
- Пустые API ключи
- Комментарии с инструкциями

### Проверить что .env НЕ в папке с .exe

```bash
# Для собранного .exe
dir dist\
# Должен быть только RapidWhisper.exe
# НЕ должно быть .env или .env.example
```

## Тестовые сценарии

### Сценарий 1: Первый запуск

1. Удалить `%APPDATA%\RapidWhisper`
2. Запустить приложение
3. Проверить приветственное окно
4. Проверить уведомление в трее
5. Проверить что открылись настройки
6. Закрыть настройки без сохранения
7. Нажать Ctrl+Space
8. Проверить что показывается уведомление об ошибке
9. Проверить что открылись настройки снова

### Сценарий 2: Установка ключа

1. Открыть настройки
2. Выбрать провайдер (Groq)
3. Вставить тестовый ключ
4. Нажать "Сохранить"
5. Проверить уведомление об успехе
6. Проверить что файл создан в AppData
7. Проверить что статус трея обновился

### Сценарий 3: Повторный запуск

1. Закрыть приложение
2. Запустить снова
3. Проверить что приветственное окно НЕ показывается
4. Проверить что настройки загрузились
5. Проверить что запись работает

### Сценарий 4: Смена провайдера

1. Открыть настройки
2. Сменить провайдер на OpenAI
3. Не вводить ключ
4. Сохранить
5. Нажать Ctrl+Space
6. Проверить что показывается ошибка
7. Проверить что открылись настройки

### Сценарий 5: Сброс настроек

1. Удалить `%APPDATA%\RapidWhisper`
2. Запустить приложение
3. Проверить что все как при первом запуске

## Проверка безопасности

### Проверить что ключи НЕ в .exe

```bash
# Открыть .exe в текстовом редакторе
notepad dist\RapidWhisper.exe

# Поискать (Ctrl+F):
# - Ваш реальный API ключ
# - "gsk_" (начало Groq ключей)
# - "sk-" (начало OpenAI ключей)

# Если нашли - НЕ РАСПРОСТРАНЯЙТЕ!
```

### Проверить содержимое .exe

```bash
# Использовать 7-Zip или аналог
# Открыть RapidWhisper.exe как архив
# Проверить что НЕТ:
# - .env
# - .env.example
# - Файлов с ключами
```

## Логи

Проверить логи на ошибки:

```bash
type rapidwhisper.log
```

Должны быть сообщения:
- "API ключ не установлен - требуется первоначальная настройка"
- "Первый запуск - требуется настройка API ключа"
- "API ключ установлен - приложение готово к работе"

## Чеклист

- [ ] Приветственное окно показывается при первом запуске
- [ ] Ссылки в окне кликабельны и открываются в браузере
- [ ] Уведомление в трее показывается
- [ ] Настройки открываются автоматически
- [ ] Запись блокируется без ключа
- [ ] Уведомление об ошибке показывается
- [ ] Настройки открываются при попытке записи
- [ ] Ключ сохраняется в AppData
- [ ] Настройки загружаются при повторном запуске
- [ ] Приветственное окно НЕ показывается повторно
- [ ] .exe НЕ содержит ключей
- [ ] .exe НЕ содержит .env файлов
- [ ] Все функции работают после установки ключа

## Результат

Если все пункты пройдены - готово к релизу! 🎉

Если есть проблемы - проверьте логи и исправьте перед распространением.
