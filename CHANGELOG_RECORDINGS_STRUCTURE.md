# Изменения: Новая структура папок записей и сохранение транскрипций

## Дата: 30.01.2026

## Описание изменений

Обновлена структура хранения записей и добавлено автоматическое сохранение транскрипций в текстовые файлы.

## Новая структура папок

### Было:
```
%APPDATA%/RapidWhisper/recordings/
  ├── recording_20260130_120000.wav
  ├── recording_20260130_120100.wav
  └── ...
```

### Стало:
```
%APPDATA%/RapidWhisper/recordings/
  ├── audio/
  │   ├── recording_20260130_120000.wav
  │   ├── recording_20260130_120100.wav
  │   └── ...
  └── transcriptions/
      ├── recording_20260130_120000.txt
      ├── recording_20260130_120100.txt
      └── ...
```

## Основные изменения

### 1. Структура папок (`core/config.py`)

Добавлены новые функции:
- `get_audio_recordings_dir()` - возвращает путь к `recordings/audio/`
- `get_transcriptions_dir()` - возвращает путь к `recordings/transcriptions/`

### 2. Сохранение транскрипций (`services/transcription_client.py`)

- Транскрипция теперь автоматически сохраняется в текстовый файл
- Имя файла транскрипции совпадает с именем аудио файла (но с расширением `.txt`)
- Кодировка: UTF-8
- Транскрипция сохраняется только если `KEEP_RECORDINGS=true` в настройках

### 3. Обновленный UI настроек (`ui/settings_window.py`)

#### Размеры окна:
- Минимальная ширина: **950px** (было 800px)
- Минимальная высота: **650px** (было 600px)
- Список записей: фиксированная высота 250-350px со скроллом

#### Список записей:
- Показывает иконку 📝 если у записи есть транскрипция
- Формат: `🎙️ recording_20260130_120000.wav 📝 | 0.45 MB | 30.01.2026 12:00:00`
- Автоматический скролл при большом количестве записей

#### Кнопки управления (компактные):
- **🔄** - Обновить список
- **▶️ Аудио** - открывает аудио файл в проигрывателе по умолчанию
- **📝 Текст** - открывает транскрипцию в текстовом редакторе
- **📁 Папка** - открывает папку `recordings` в проводнике
- **🗑️ Удалить** - удаляет и аудио, и транскрипцию

#### Tooltip для папки:
```
Кликните чтобы открыть папку

Аудио: recordings/audio/
Транскрипции: recordings/transcriptions/
```

## Поведение

### При включенной настройке `KEEP_RECORDINGS=true`:

1. После успешной транскрипции:
   - Аудио файл перемещается в `recordings/audio/recording_YYYYMMDD_HHMMSS.wav`
   - Транскрипция сохраняется в `recordings/transcriptions/recording_YYYYMMDD_HHMMSS.txt`

2. При удалении записи через UI:
   - Удаляется и аудио файл, и файл транскрипции (если есть)

3. При открытии транскрипции:
   - Если транскрипции нет, показывается сообщение "У этой записи нет транскрипции"
   - Если есть, открывается в текстовом редакторе по умолчанию

## Обратная совместимость

- Старые записи в папке `recordings/` (без подпапок) продолжат отображаться
- Новые записи будут сохраняться в новую структуру `recordings/audio/` и `recordings/transcriptions/`
- Рекомендуется вручную переместить старые файлы в `recordings/audio/` для единообразия

## Тестирование

Для проверки работы:

1. Включите `KEEP_RECORDINGS=true` в настройках
2. Сделайте запись и дождитесь транскрипции
3. Откройте настройки → Записи
4. Проверьте что запись отображается с иконкой 📝
5. Попробуйте открыть аудио и текст
6. Проверьте что оба файла находятся в правильных папках
7. Проверьте что список записей скроллится если их много

## Файлы изменены

- `core/config.py` - добавлены функции для новых папок
- `services/transcription_client.py` - добавлено сохранение транскрипции
- `ui/settings_window.py` - обновлен UI для работы с транскрипциями, увеличены размеры окна, добавлен скролл
