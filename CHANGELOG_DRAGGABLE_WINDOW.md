# Changelog: Перетаскивание и запоминание позиции окна

## Дата: 30 января 2026

## Описание изменений

Добавлена возможность перетаскивать окно записи мышкой и автоматически сохранять его позицию для следующих запусков.

## Новые возможности

### 1. Перетаскивание окна мышкой
- ✅ Можно перетащить окно в любое место экрана во время записи
- ✅ Окно остается всегда поверх других приложений
- ✅ Визуальная обратная связь - курсор меняется на "руку"
- ✅ Плавное перемещение без задержек

### 2. Автоматическое сохранение позиции
- ✅ Позиция сохраняется автоматически после перетаскивания
- ✅ При следующей записи окно появится в том же месте
- ✅ Настройка включена по умолчанию

### 3. Настройка запоминания позиции
- ✅ Можно включить/выключить через .env файл
- ✅ Если выключено - окно всегда появляется по центру
- ✅ Если включено - окно появляется в последней позиции

## Как использовать?

### Перетаскивание окна

1. Нажмите Ctrl+Space для начала записи
2. Окно появится (по центру или в сохраненной позиции)
3. Наведите курсор на окно - курсор изменится на "открытую руку" ✋
4. Нажмите левую кнопку мыши и удерживайте - курсор изменится на "закрытую руку" ✊
5. Перетащите окно в нужное место
6. Отпустите кнопку мыши - позиция автоматически сохранится

### Настройка запоминания позиции

Откройте `.env` файл и найдите секцию:

```env
# ============================================
# Window Position (OPTIONAL)
# ============================================
# Remember window position after dragging
# Default: true
# Options: true, false
REMEMBER_WINDOW_POSITION=true

# Window position (automatically saved when you drag the window)
# Leave empty for center position
WINDOW_POSITION_X=
WINDOW_POSITION_Y=
```

**Опции:**

- `REMEMBER_WINDOW_POSITION=true` - запоминать позицию (по умолчанию)
- `REMEMBER_WINDOW_POSITION=false` - всегда показывать по центру

**Примечание:** Поля `WINDOW_POSITION_X` и `WINDOW_POSITION_Y` заполняются автоматически при перетаскивании окна.

## Технические детали

### Реализация перетаскивания

```python
def mousePressEvent(self, event) -> None:
    """Начало перетаскивания"""
    if event.button() == Qt.MouseButton.LeftButton:
        self._is_dragging = True
        self._drag_position = event.globalPosition().toPoint() - self.frameGeometry().topLeft()
        self.setCursor(Qt.CursorShape.ClosedHandCursor)

def mouseMoveEvent(self, event) -> None:
    """Перемещение окна"""
    if self._is_dragging and self._drag_position is not None:
        new_pos = event.globalPosition().toPoint() - self._drag_position
        self.move(new_pos)

def mouseReleaseEvent(self, event) -> None:
    """Завершение перетаскивания и сохранение позиции"""
    if event.button() == Qt.MouseButton.LeftButton:
        self._is_dragging = False
        self.setCursor(Qt.CursorShape.ArrowCursor)
        self.save_position()  # Автоматическое сохранение
```

### Сохранение позиции

Позиция сохраняется в `.env` файл:

```python
def save_position(self) -> None:
    """Сохраняет текущую позицию окна"""
    pos = self.pos()
    x, y = pos.x(), pos.y()
    
    # Обновить .env файл
    env_lines[i] = f'WINDOW_POSITION_X={x}\n'
    env_lines[j] = f'WINDOW_POSITION_Y={y}\n'
```

### Загрузка позиции

```python
def load_position(self) -> tuple[int, int] | None:
    """Загружает сохраненную позицию"""
    x = os.getenv('WINDOW_POSITION_X')
    y = os.getenv('WINDOW_POSITION_Y')
    
    if x is not None and y is not None:
        return (int(x), int(y))
    return None
```

### Показ окна с учетом настройки

```python
def show_at_center(self, use_saved_position: bool = True) -> None:
    """Показывает окно"""
    saved_pos = None
    if use_saved_position:
        saved_pos = self.load_position()
    
    if saved_pos:
        x, y = saved_pos
        self.move(x, y)
    else:
        # Центрировать окно
        ...
```

## Визуальная обратная связь

### Курсоры мыши

| Состояние | Курсор | Описание |
|-----------|--------|----------|
| Обычное | Стрелка ➡️ | Курсор не на окне |
| Наведение | Открытая рука ✋ | Можно начать перетаскивание |
| Перетаскивание | Закрытая рука ✊ | Окно перемещается |
| После отпускания | Стрелка ➡️ | Позиция сохранена |

## Измененные файлы

### 1. ui/floating_window.py
- Добавлены переменные `_drag_position` и `_is_dragging`
- Добавлены методы `mousePressEvent()`, `mouseMoveEvent()`, `mouseReleaseEvent()`
- Обновлены методы `enterEvent()` и `leaveEvent()` для смены курсора
- Добавлены методы `save_position()` и `load_position()`
- Обновлен метод `show_at_center()` с параметром `use_saved_position`

### 2. core/config.py
- Добавлено поле `remember_window_position: bool`
- Добавлены поля `window_position_x` и `window_position_y`
- Обновлен метод `load_from_env()` для загрузки настроек позиции
- Обновлен `create_default_env()` с новыми полями

### 3. main.py
- Добавлен метод `_show_window_with_config()`
- Обновлено подключение сигнала `_show_window_signal`

### 4. .env.example
- Добавлена секция "Window Position"
- Добавлены поля `REMEMBER_WINDOW_POSITION`, `WINDOW_POSITION_X`, `WINDOW_POSITION_Y`

## Сценарии использования

### Сценарий 1: Работа в правом углу экрана
1. Пользователь предпочитает видеть окно в правом верхнем углу
2. При первой записи перетаскивает окно туда
3. При всех следующих записях окно появляется в правом углу
4. Удобно для работы с документами слева

### Сценарий 2: Работа на втором мониторе
1. Пользователь работает на двух мониторах
2. Перетаскивает окно на второй монитор
3. Окно запоминает позицию на втором мониторе
4. При следующих записях окно появляется там же

### Сценарий 3: Центрированное окно
1. Пользователь хочет всегда видеть окно по центру
2. Устанавливает `REMEMBER_WINDOW_POSITION=false`
3. Окно всегда появляется по центру экрана
4. Можно перетаскивать, но позиция не сохраняется

### Сценарий 4: Сброс позиции
1. Пользователь переместил окно за пределы экрана (сменил разрешение)
2. Открывает `.env` файл
3. Удаляет значения `WINDOW_POSITION_X` и `WINDOW_POSITION_Y`
4. Окно снова появляется по центру

## Преимущества

### Для пользователя:
- 🎯 Окно всегда там, где удобно
- 🎯 Не нужно каждый раз перемещать
- 🎯 Гибкая настройка поведения
- 🎯 Работает на нескольких мониторах

### Для UX:
- ✨ Запоминает предпочтения пользователя
- ✨ Интуитивное перетаскивание
- ✨ Визуальная обратная связь (курсоры)
- ✨ Автоматическое сохранение

## Совместимость

- ✅ Windows 10/11 - полная поддержка
- ✅ macOS 10.14+ - полная поддержка
- ✅ Linux (X11) - полная поддержка
- ✅ Linux (Wayland) - полная поддержка
- ✅ Мультимониторные конфигурации

## Тестирование

### Как проверить:

1. Запустите приложение
2. Нажмите Ctrl+Space
3. Наведите курсор на окно - должна появиться "открытая рука"
4. Перетащите окно в другое место
5. Отпустите кнопку мыши
6. Нажмите ESC для отмены записи
7. Нажмите Ctrl+Space снова
8. Окно должно появиться в том же месте

### Проверка настройки:

1. Откройте `.env` файл
2. Найдите `REMEMBER_WINDOW_POSITION=true`
3. Измените на `false`
4. Перезапустите приложение
5. Окно должно появляться по центру

## Известные ограничения

1. **Смена разрешения экрана**: Если изменить разрешение, сохраненная позиция может оказаться за пределами экрана
   - **Решение**: Удалить значения `WINDOW_POSITION_X/Y` из .env

2. **Отключение монитора**: Если окно было на втором мониторе, который отключили
   - **Решение**: Окно автоматически переместится на основной монитор (Qt делает это автоматически)

3. **Очень маленькие экраны**: На экранах < 800x600 окно может не поместиться
   - **Решение**: Окно автоматически центрируется

## Будущие улучшения

Возможные улучшения в будущих версиях:

1. **Привязка к углам**: Автоматическая привязка к углам экрана при перетаскивании
2. **Настройка в UI**: Добавить настройку в окно настроек (не только в .env)
3. **Несколько позиций**: Сохранять разные позиции для разных мониторов
4. **Горячие клавиши**: Быстрое перемещение окна клавишами (Ctrl+Arrow)

## Связанные изменения

- Окно всегда поверх (см. CHANGELOG_ALWAYS_ON_TOP.md)
- Правильные курсоры (см. CHANGELOG_CURSORS.md)
- Адаптивное окно настроек (см. CHANGELOG_ABOUT_SECTION.md)

---

**Версия:** 1.3.0  
**Статус:** ✅ Готово к использованию  
**Приоритет:** 🔥 Высокий (улучшает UX)
