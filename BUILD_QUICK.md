# Быстрая сборка в .exe

## ⚠️ ВАЖНО: Ваши ключи в безопасности!

**build.bat автоматически создает чистый .env без ваших API ключей!**

Ваш .env с ключами:
1. Сохраняется в .env.backup
2. Заменяется на чистый (без ключей)
3. Собирается .exe
4. Восстанавливается обратно

**Результат**: .exe НЕ содержит ваших ключей ✅

## Самый простой способ

1. **Запустите build.bat**
   ```
   Двойной клик на build.bat
   ```

2. **Дождитесь завершения**
   - Установка PyInstaller (если нужно)
   - Очистка старых файлов
   - Сборка .exe

3. **Готово!**
   ```
   Файл: dist\RapidWhisper.exe
   ```

## Ручная сборка

```bash
# 1. Установить PyInstaller
uv pip install pyinstaller

# 2. Собрать
uv run pyinstaller RapidWhisper.spec

# 3. Результат
dist\RapidWhisper.exe
```

## Что дальше?

1. Скопируйте `dist\RapidWhisper.exe` в новую папку
2. Скопируйте `.env` с вашими API ключами
3. Запустите RapidWhisper.exe
4. Готово! 🎉

## Проблемы?

Смотрите подробную инструкцию: `docs/BUILD_EXE.md`
