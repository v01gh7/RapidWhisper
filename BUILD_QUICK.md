# 🚀 Быстрая сборка RapidWhisper

## Простая сборка в .exe

```bash
# Запустите build.bat
build.bat
```

Готово! Файл будет в `dist\RapidWhisper.exe`

## Что делает build.bat?

1. ✅ Проверяет PyInstaller
2. 💾 Сохраняет ваш .env (с ключами)
3. 🔨 Собирает чистый .exe (БЕЗ .env файлов)
4. ♻️ Восстанавливает ваш .env

## Важно!

- ✅ В .exe **НЕТ** .env файлов
- ✅ Настройки сохраняются в `%APPDATA%\RapidWhisper\.env`
- ✅ При первом запуске пользователь увидит инструкцию
- ✅ Нужно будет установить API ключ через настройки

## Для распространения

Нужен только один файл:
- `dist\RapidWhisper.exe`

Всё! Больше ничего не нужно.

## Первый запуск у пользователя

1. Запускает `RapidWhisper.exe`
2. Видит приветственное окно с инструкциями
3. Получает бесплатный API ключ (ссылки в окне)
4. Открывает настройки и добавляет ключ
5. Готово к работе!

## Проверка перед релизом

См. `DISTRIBUTION_CHECKLIST.md`
