# Changelog: Предустановленные позиции окна

## Дата: 30 января 2026

## Описание изменений

Добавлен выпадающий список с предустановленными позициями для окна записи. Теперь можно быстро выбрать, где будет появляться окно, без необходимости перетаскивать его вручную.

## Новые возможности

### Предустановленные позиции

Доступны следующие позиции:

1. **Центр** - окно появляется по центру экрана (по умолчанию)
2. **Левый верхний угол** - окно в левом верхнем углу с отступом 20px
3. **Правый верхний угол** - окно в правом верхнем углу с отступом 20px
4. **Левый нижний угол** - окно в левом нижнем углу с отступом 20px
5. **Правый нижний угол** - окно в правом нижнем углу с отступом 20px
6. **Пользовательская** - окно в позиции, куда вы его перетащили

## Как использовать?

### Выбор предустановленной позиции:

1. Откройте настройки
2. Перейдите в раздел "⚡ Приложение"
3. Найдите группу "Интерфейс"
4. **Снимите** галочку "Запоминать позицию окна"
5. В выпадающем списке "Позиция окна" выберите нужную позицию
6. Нажмите "💾 Сохранить"

Теперь окно записи будет появляться в выбранной позиции.

### Использование пользовательской позиции:

1. Откройте настройки
2. **Поставьте** галочку "Запоминать позицию окна"
3. Нажмите "💾 Сохранить"
4. Начните запись (Ctrl+Space)
5. Перетащите окно в нужное место
6. При следующей записи окно появится там же

Выпадающий список автоматически переключится на "Пользовательская".

## Логика работы

### Когда чекбокс ВЫКЛЮЧЕН (❌):
- Выпадающий список **активен**
- Можно выбрать предустановленную позицию
- Окно всегда появляется в выбранной позиции
- Перетаскивание не сохраняется

### Когда чекбокс ВКЛЮЧЕН (✅):
- Выпадающий список **неактивен** (серый)
- Используется пользовательская позиция
- Окно появляется там, куда вы его перетащили
- При перетаскивании позиция автоматически сохраняется

## Технические детали

### Предустановленные позиции

```python
if preset == 'center':
    x = geometry.center().x() - self.window_width // 2
    y = geometry.center().y() - self.window_height // 2
elif preset == 'top_left':
    x = geometry.left() + margin  # margin = 20px
    y = geometry.top() + margin
elif preset == 'top_right':
    x = geometry.right() - self.window_width - margin
    y = geometry.top() + margin
elif preset == 'bottom_left':
    x = geometry.left() + margin
    y = geometry.bottom() - self.window_height - margin
elif preset == 'bottom_right':
    x = geometry.right() - self.window_width - margin
    y = geometry.bottom() - self.window_height - margin
```

### Автоматическое переключение на "custom"

При перетаскивании окна (если чекбокс включен):

```python
def save_position(self) -> None:
    # Сохранить координаты
    x, y = pos.x(), pos.y()
    
    # Автоматически установить preset в "custom"
    env_lines[i] = f'WINDOW_POSITION_PRESET=custom\n'
```

### Настройки в .env

```env
# Запоминать позицию
REMEMBER_WINDOW_POSITION=true

# Предустановленная позиция
WINDOW_POSITION_PRESET=center

# Пользовательские координаты (для preset=custom)
WINDOW_POSITION_X=
WINDOW_POSITION_Y=
```

## Измененные файлы

### 1. ui/settings_window.py
- Добавлен `QComboBox` с предустановленными позициями
- Добавлен метод `_on_remember_position_changed()` для управления состоянием списка
- Обновлен метод `_load_values()` для загрузки preset
- Обновлен метод `_save_settings()` для сохранения preset

### 2. ui/floating_window.py
- Обновлен метод `show_at_center()` с логикой предустановленных позиций
- Обновлен метод `save_position()` для автоматической установки preset="custom"

### 3. core/config.py
- Добавлено поле `window_position_preset: str`
- Обновлен метод `load_from_env()` для загрузки preset
- Обновлен `create_default_env()` с новым полем

### 4. .env.example
- Добавлено поле `WINDOW_POSITION_PRESET`
- Обновлены комментарии

## Сценарии использования

### Сценарий 1: Работа в правом верхнем углу
```
1. Открыть настройки
2. Снять галочку "Запоминать позицию окна"
3. Выбрать "Правый верхний угол"
4. Сохранить
5. Окно всегда будет в правом верхнем углу
```

### Сценарий 2: Работа по центру
```
1. Открыть настройки
2. Снять галочку "Запоминать позицию окна"
3. Выбрать "Центр"
4. Сохранить
5. Окно всегда будет по центру
```

### Сценарий 3: Пользовательская позиция
```
1. Открыть настройки
2. Поставить галочку "Запоминать позицию окна"
3. Сохранить
4. Начать запись
5. Перетащить окно в нужное место
6. Окно запомнит эту позицию
```

### Сценарий 4: Переключение между режимами
```
1. Использовали пользовательскую позицию
2. Решили переключиться на предустановленную
3. Открыть настройки
4. Снять галочку "Запоминать позицию окна"
5. Выбрать нужную предустановленную позицию
6. Сохранить
```

## Преимущества

### Для пользователя:
- 🎯 Быстрый выбор позиции без перетаскивания
- 🎯 Стандартные позиции для разных сценариев работы
- 🎯 Гибкость - можно использовать и предустановленные, и пользовательские позиции
- 🎯 Интуитивный интерфейс

### Для UX:
- ✨ Меньше действий для настройки
- ✨ Предсказуемое поведение
- ✨ Визуальная обратная связь (активный/неактивный список)
- ✨ Автоматическое переключение на "custom" при перетаскивании

## Визуальное поведение

### Состояние элементов

| Чекбокс | Выпадающий список | Поведение |
|---------|-------------------|-----------|
| ❌ Выключен | ✅ Активен | Используется выбранная предустановленная позиция |
| ✅ Включен | ❌ Неактивен (серый) | Используется пользовательская позиция |

### Автоматическое переключение

```
1. Чекбокс включен, список неактивен
2. Пользователь перетаскивает окно
3. Позиция сохраняется
4. Preset автоматически меняется на "custom"
5. При открытии настроек список показывает "Пользовательская"
```

## Совместимость

- ✅ Windows 10/11 - полная поддержка
- ✅ macOS 10.14+ - полная поддержка
- ✅ Linux (все дистрибутивы) - полная поддержка
- ✅ Мультимониторные конфигурации

### Особенности на разных экранах

- **Маленькие экраны** (< 1366x768): Отступы 20px могут быть слишком большими
- **Большие экраны** (> 1920x1080): Углы могут быть далеко от центра
- **Мультимониторы**: Позиции рассчитываются для основного монитора

## Тестирование

### Проверка предустановленных позиций:

1. Откройте настройки
2. Снимите галочку "Запоминать позицию окна"
3. Выберите каждую позицию по очереди
4. Сохраните и проверьте, что окно появляется в правильном месте

### Проверка пользовательской позиции:

1. Включите галочку "Запоминать позицию окна"
2. Перетащите окно в произвольное место
3. Откройте настройки
4. Проверьте, что список показывает "Пользовательская"

### Проверка переключения:

1. Используйте пользовательскую позицию
2. Переключитесь на предустановленную
3. Проверьте, что окно появляется в новой позиции
4. Переключитесь обратно на пользовательскую
5. Проверьте, что окно появляется в сохраненной позиции

## Известные ограничения

1. **Отступы фиксированные**: 20px от краев экрана (не настраивается)
2. **Только основной монитор**: Предустановленные позиции работают только на основном мониторе
3. **Нет проверки границ**: Если окно выходит за пределы экрана, Qt автоматически переместит его

## Будущие улучшения

Возможные улучшения в будущих версиях:

1. **Настраиваемые отступы**: Позволить пользователю выбрать отступ от краев
2. **Больше позиций**: Добавить позиции по центру каждой стороны (верх, низ, лево, право)
3. **Визуальный предпросмотр**: Показывать миниатюру экрана с позицией окна
4. **Мультимониторная поддержка**: Выбор монитора для каждой позиции

## Связанные изменения

- Перетаскивание окна (см. CHANGELOG_DRAGGABLE_WINDOW.md)
- Запоминание позиции (см. UPDATE_WINDOW_POSITION.md)
- Окно всегда поверх (см. CHANGELOG_ALWAYS_ON_TOP.md)

---

**Версия:** 1.3.0  
**Статус:** ✅ Готово к использованию  
**Приоритет:** 🔥 Высокий (улучшает UX)
